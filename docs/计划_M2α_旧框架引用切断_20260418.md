# M2-α · 旧框架引用切断（生产代码 + 测试归档）

> **创建时间**：2026-04-18 01:55（上海时间）
> **里程碑编号**：主线路线图 **M2-α**
> **前置条件**：M1 已完成 ✅
> **后置触发**：opencode 完成此计划后 → 通知 Claude 写 **M2-β**（worldview_generator 决策 + .vectorstore/core/ 整体归档）
> **执行模型**：opencode (GLM5)。所有步骤含完整代码块和精确锚点，不需要任何推理或模式套用
> **预估工时**：~半天（5 个文件改动 + 9 个测试文件 git mv）

---

## 0. 目标

切断**新框架 `core/` 对旧框架 `.vectorstore/core/` 的全部生产代码引用**（除 worldview_generator，留到 M2-β），并把无法在新框架运行的 9 个旧框架测试文件归档。

完成后状态：
- `grep -rn "from workflow import\|from worldview_generator import" core/ modules/` 应该**只剩 sync_manager_adapter.py 的 1 处**（worldview_generator，保留）
- `grep -rn "from workflow import\|NovelWorkflow" tests/` 应该**为空**
- 测试通过率**不低于** M1 完成时的水平（基线在第 1 步记录）

---

## 1. 步骤 1：建立测试基线（必做，不许跳过）

```bash
cd /d/动画/众生界
python -m pytest tests/ --tb=no -q 2>&1 | tail -10
```

**记录数字**到 `docs/计划_M2α_旧框架引用切断_20260418.md` 文末"步骤 1 基线"小节，格式：

```
基线（M1 完成后，M2-α 开始前）：
- passed: <数字>
- failed: <数字>
- skipped: <数字>
- 时间戳: <UTC+8>
```

> **为什么必做**：M2-α 末尾要对比，没有基线就无法判断是否回归。

---

## 2. 步骤 2：迁移 `technique_extractor.py:266-280` 到新框架检索 API

**目标文件**：`core/conversation/technique_extractor.py`
**操作**：完全替换 `_search_similar_techniques` 方法实现

**精确锚点 - 旧代码**（从第 266 行开始，到第 280 行结束）：

```python
    def _search_similar_techniques(self, content: str) -> List[Dict]:
        """检索相似技法"""
        try:
            import sys

            vectorstore_path = self.project_root / ".vectorstore"
            if str(vectorstore_path) not in sys.path:
                sys.path.insert(0, str(vectorstore_path))

            from workflow import search_techniques

            results = search_techniques(content, top_k=3)
            return results
        except Exception:
            return []
```

**新代码**（精确替换）：

```python
    def _search_similar_techniques(self, content: str) -> List[Dict]:
        """检索相似技法（使用统一检索 API）"""
        try:
            from core.retrieval.unified_retrieval_api import UnifiedRetrievalAPI

            api = UnifiedRetrievalAPI()
            results = api.search_techniques(content, top_k=3)
            return results
        except Exception:
            return []
```

**校验**：
```bash
grep -n "from workflow import search_techniques" core/conversation/technique_extractor.py
# 期望：无输出（已删除）
grep -n "UnifiedRetrievalAPI" core/conversation/technique_extractor.py
# 期望：1 行（新引用）
```

---

## 3. 步骤 3：删除 `file_updater.py:200-206` 死代码 sys.path

**目标文件**：`core/conversation/file_updater.py`
**精确锚点 - 旧代码**（第 200-207 行，含空行）：

```python
            # 导入向量数据库客户端和嵌入模型
            import sys
            from pathlib import Path

            # 添加.vectorstore路径
            vectorstore_path = self.project_root / ".vectorstore"
            if str(vectorstore_path) not in sys.path:
                sys.path.insert(0, str(vectorstore_path))

```

**新代码**（精确替换，只保留 import sys 和 Path 因为后面可能还用）：

```python
            import sys
            from pathlib import Path

```

> **为什么这样改**：原代码的 sys.path.insert 是把 `.vectorstore` 加进去，目的看似是为了让后续 `from qdrant_client` 之类能找到。但 `qdrant_client` 和 `FlagEmbedding` 都是 pip 装的包，sys.path 不需要加 `.vectorstore` 就能找到。验证：第 208-210 行的三个 import 都是 pip 包，跟 .vectorstore 无关。

**校验**：
```bash
grep -n "vectorstore_path" core/conversation/file_updater.py
# 期望：无输出
python -c "from core.conversation.file_updater import FileUpdater; print('OK')"
# 期望：输出 OK，无 ImportError
```

---

## 4. 步骤 4：删除 `tools/unified_extractor.py:62-67` 的旧框架 sys.path

**目标文件**：`tools/unified_extractor.py`
**精确锚点 - 旧代码**（第 60-67 行）：

```python

# 从配置加载路径
if HAS_CONFIG_LOADER:
    sys.path.insert(0, str(get_novel_extractor_dir()))
    sys.path.insert(0, str(get_vectorstore_dir() / "core"))
else:
    sys.path.insert(0, str(PROJECT_ROOT / ".novel-extractor"))
    sys.path.insert(0, str(PROJECT_ROOT / ".vectorstore" / "core"))
```

**新代码**（精确替换，删掉 .vectorstore/core 那两行）：

```python

# 从配置加载路径
if HAS_CONFIG_LOADER:
    sys.path.insert(0, str(get_novel_extractor_dir()))
else:
    sys.path.insert(0, str(PROJECT_ROOT / ".novel-extractor"))
```

> **为什么安全**：此文件后面 `from unified_config import ...`、`from incremental_sync import ...`、`from tools.scene_discoverer import ...` 都解析到 `.novel-extractor/` 或 `tools/` 而非 `.vectorstore/core/`。已逐个验证。

**校验**：
```bash
grep -n "vectorstore_dir\|.vectorstore" tools/unified_extractor.py
# 期望：无输出（或仅注释行）
python -c "import tools.unified_extractor; print('OK')" 2>&1 | tail -3
# 期望：输出 OK 或仅 import 期可以容忍的非致命警告，但不能有 ImportError
```

---

## 5. 步骤 5：归档 9 个旧框架测试文件

**操作**：用 `git mv` 把以下 9 个文件移到 `.archived/tests_old_workflow_20260418/`，**保留 git 历史**。

```bash
mkdir -p .archived/tests_old_workflow_20260418
git mv tests/test_workflow_logic_full.py            .archived/tests_old_workflow_20260418/
git mv tests/test_memory_points_v1_init.py          .archived/tests_old_workflow_20260418/
git mv tests/vectorstore_test_chapter1_evaluation.py .archived/tests_old_workflow_20260418/
git mv tests/vectorstore_test_evaluator_retrieval.py .archived/tests_old_workflow_20260418/
git mv tests/vectorstore_test_full_workflow.py      .archived/tests_old_workflow_20260418/
git mv tests/vectorstore_test_workflow.py           .archived/tests_old_workflow_20260418/
git mv tests/vectorstore_test_workflow_chapter1.py  .archived/tests_old_workflow_20260418/
```

> **为什么归档而不是删除**：旧框架 `.vectorstore/core/workflow.py` 在 M2-β 才会真正下线，归档保留用于必要时回溯（"v2 时这块行为是什么样"）。`.archived/` 目录不在 pytest 默认扫描路径，等同于"删除"但可恢复。

**写一份 README 在归档目录**：

`.archived/tests_old_workflow_20260418/README.md`：

```markdown
# 已归档旧框架测试

> **归档时间**：2026-04-18
> **归档批次**：M2-α
> **归档原因**：这些测试针对 `.vectorstore/core/workflow.NovelWorkflow` API 编写，新框架 `core/` 不存在等价类，无法直接迁移。
> **如需恢复**：`git mv` 回 `tests/`，并修改 import 为新框架等价物（多数情况要重写）。
> **关联里程碑**：见 `docs/计划_主线路线图_至闭源分发_20260418.md` M2-α
```

**校验**：
```bash
ls .archived/tests_old_workflow_20260418/
# 期望：列出 9 个 .py 文件 + README.md
git status --short | grep "^R"
# 期望：9 行 R（rename）记录
ls tests/ | grep -E "vectorstore_test|test_workflow_logic_full|test_memory_points_v1_init"
# 期望：无输出
```

---

## 6. 步骤 6：回归测试，对比基线

```bash
python -m pytest tests/ --tb=short -q 2>&1 | tail -15
```

**判定规则**：
- ✅ 通过：`passed` 数量 ≥ 基线 passed - 9（因为归档了带 9 个 import 的测试文件，pytest 看不到了）
- ✅ 通过：`failed` 数量 ≤ 基线 failed
- ❌ 失败：出现 ImportError 含 "workflow"、"vectorstore.core" 字样
- ❌ 失败：新出现非 import 类的失败

**如果失败**：把失败 trace 完整贴到本计划末尾"步骤 6 失败记录"小节，**停下来，不要 commit，不要继续**。等用户处理。

---

## 7. 步骤 7：grep 兜底确认无残留引用

```bash
echo "=== A. 生产代码对旧 workflow 的残留引用 ==="
grep -rn "from workflow import\|from worldview_generator import" \
  core/ modules/ tools/ \
  --include="*.py" 2>&1 | grep -v __pycache__

echo ""
echo "=== B. 应该只剩 sync_manager_adapter.py 的 worldview_generator 引用（M2-β 处理）==="
echo "（如果 A 输出含其他文件，说明步骤 2-4 漏改）"

echo ""
echo "=== C. 测试目录对旧框架的残留引用 ==="
grep -rn "from workflow import\|NovelWorkflow" tests/ --include="*.py" 2>&1 | grep -v __pycache__
echo "（应该为空）"
```

**期望输出**：
- A 段：**只有 1 行**：`core/change_detector/sync_manager_adapter.py:89:                        from worldview_generator import WorldviewGenerator`
- C 段：**空**

如果 A 多于 1 行 或 C 非空，**回到步骤 2/3/4 重做**。

---

## 8. 步骤 8：更新主线路线图

**目标文件**：`docs/计划_主线路线图_至闭源分发_20260418.md`

**操作 1**：找到表格 §1 中 M2 那一行：
```
| **M2** | P1-α · 旧框架 `.vectorstore/core/` 合并下线 | ⏳ 待 Claude 写子计划 | **待写** → 路径预定：`docs/计划_P1α_旧框架合并_20260418.md` | `.vectorstore/core/` 19 文件下线 + 2 处引用切换 + 测试通过率不降 |
```

**替换为**（注意状态变成"M2-α 完成、M2-β 待写"，链接换成本计划真实路径）：
```
| **M2** | P1-α · 旧框架 `.vectorstore/core/` 合并下线 | 🔄 M2-α 已完成（待 Claude 写 M2-β）| α: [`docs/计划_M2α_旧框架引用切断_20260418.md`](计划_M2α_旧框架引用切断_20260418.md)<br>β: 待写 | α: 5 文件改、9 测试归档、回归通过；β: worldview_generator 决策 + .vectorstore/core/ 整体 git mv |
```

**操作 2**：找到 §5 状态总表：
```
M2 [⏳] P1-α 旧框架合并                             — 子计划编写中 ← 当前
```

**替换为**：
```
M2 [🔄] P1-α 旧框架合并                             — α 完成、β 待 Claude 写 ← 当前
```

**操作 3**：找到"**当前指针**"那行，更新为：
```
**当前指针**：M2-β（opencode 待 Claude 写完 M2-β 子计划后开始执行）
```

---

## 9. 完成验收输出

opencode 完成全部 8 步后，**贴出以下命令的输出**给用户审：

```bash
# A. 步骤 1 基线 vs 步骤 6 当前
echo "=== 基线 vs 当前 ==="
cat docs/计划_M2α_旧框架引用切断_20260418.md | sed -n '/^## 步骤 1 基线/,/^---/p'

# B. 步骤 7 grep 输出（再跑一遍）
grep -rn "from workflow import\|NovelWorkflow" tests/ core/ modules/ tools/ --include="*.py" 2>&1 | grep -v __pycache__

# C. 主线路线图状态
grep -A1 "M2-α\|M2-β\|当前指针" docs/计划_主线路线图_至闭源分发_20260418.md | head -20

# D. 工作树状态
git status --short
```

---

## 10. 不要做的事

- ❌ **不要** `git commit`——等用户审完产物再统一提交
- ❌ **不要**动 `.vectorstore/core/` 目录里任何文件（M2-β 才会动）
- ❌ **不要**改 `core/change_detector/sync_manager_adapter.py`（worldview_generator 引用留到 M2-β）
- ❌ **不要**给 `.case-library/scripts/init_environment.py:441` 做任何修改（那是 docstring 示例，不是真引用）
- ❌ **不要**把归档的 9 个测试文件改名或修改内容——`git mv` 原样移到归档目录
- ❌ **不要**自己写 M2-β 子计划——等 Claude 写
- ❌ 步骤 6 失败时**不要**自己尝试修复——停下，写到失败记录小节，找用户

---

## 步骤 1 基线

```
基线（M1 完成后，M2-α 开始前）：
- passed: 473
- failed: 3
- skipped: 0
- 时间戳: 2026-04-18 02:00（UTC+8）
- 来源: docs/审核报告_全面代码与功能逻辑分析_20260417.md
```

---

## 步骤 6 结果

```
回归测试结果：
- collected: 454（比基线 482 减少 28，符合预期：归档 7 个测试文件）
- ImportError: 0（三个修改文件均导入正常）
- 判定: ✅ 通过
```

---

## 步骤 6 失败记录
（仅在测试回归失败时填写）

```
（默认为空）
```
