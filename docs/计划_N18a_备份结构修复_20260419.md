> ⚠️ **DEPRECATED** — 本计划已执行完毕，对应任务全部完成。此文件仅作历史存档，请勿据此执行任何操作。
> 归档时间：2026-04-21（Asia/Shanghai）

---

# 计划 P0-1 / N18a:skill_backup_20260418 备份结构修复

- **创建时间**:2026-04-19 (Asia/Shanghai)
- **执行者**:opencode (GLM5)
- **路线图位置**:[ROADMAP_众生界v2实施_20260419.md](./ROADMAP_众生界v2实施_20260419.md) §3.1 P0-1
- **依据**:[m7_artifacts/m7_summary.md](./m7_artifacts/m7_summary.md) §3 N18 残留 #2

---

## 0. 本计划的目的(给 opencode 与未来 Claude)

2026-04-18 opencode 执行 N18 时,把 `novel-workflow/` 的内容(SKILL.md / SKILL.md.bak / REFACTOR.md / SPEC.md / templates/)**错误地铺平在 `docs/m7_artifacts/skill_backup_20260418/` 顶层**,而不是放入 `novel-workflow/` 子目录,导致:

- 顶层只有 **7 个 skill 子目录**(缺 `novel-workflow/`),违反 [N18-A] "8 个子目录"。
- 与其他 skill 的备份结构不一致,失去对称性。
- 未来 N18 扩展(P0-4 清理剩余 4 个 skill)时会重复犯错。

**本计划只做 1 件事**:把顶层 4 个文件 + `templates/` 目录**移入** `novel-workflow/` 子目录,使顶层恰好剩 8 个 skill 子目录(7 个原有 + 1 个新建的 novel-workflow)。

---

## 1. 执行前的真实状态(Claude 已于 2026-04-19 核验)

命令:`ls docs/m7_artifacts/skill_backup_20260418/`

真实输出:
```
REFACTOR.md              ← novel-workflow 文件(错位在顶层)
SKILL.md                 ← novel-workflow 文件(错位)
SKILL.md.bak             ← novel-workflow 文件(错位)
SPEC.md                  ← novel-workflow 文件(错位)
novelist-canglan/        ← 正确
novelist-evaluator/      ← 正确
novelist-jianchen/       ← 正确
novelist-moyan/          ← 正确
novelist-worldview-generator/   ← 正确
novelist-xuanyi/         ← 正确
novelist-yunxi/          ← 正确
templates/               ← novel-workflow 的子目录(错位在顶层)
```

预期操作后:
```
novel-workflow/          ← 新建,含下列 5 个条目
    REFACTOR.md
    SKILL.md
    SKILL.md.bak
    SPEC.md
    templates/
novelist-canglan/        ← 不动
novelist-evaluator/      ← 不动
novelist-jianchen/       ← 不动
novelist-moyan/          ← 不动
novelist-worldview-generator/   ← 不动
novelist-xuanyi/         ← 不动
novelist-yunxi/          ← 不动
```

顶层恰好 **8 个子目录,0 个散文件**。

---

## 2. opencode 执行规则(不可违反)

### 2.1 允许的操作

1. **1 次 mkdir**:`mkdir docs/m7_artifacts/skill_backup_20260418/novel-workflow`
2. **5 次 mv**:把顶层 4 个文件 + `templates/` 目录移入 `novel-workflow/`
3. **1 次 ls 自检**:确认结果

### 2.2 禁止的操作

- ❌ **禁止 cp**(用 mv,不要留副本)
- ❌ **禁止改任何文件内容**
- ❌ **禁止动 7 个已有 skill 子目录的任何内容**
- ❌ **禁止动 `~/.agents/skills/` 下任何文件**
- ❌ **禁止 git commit**
- ❌ **禁止创建、删除、或修改本计划之外的任何文件**
- ❌ **不要"顺便"处理其他残留(比如 n18_test_log.txt 或 4 个未覆盖 skill),那些是 P0-2 / P0-4 的工作,不在本计划范围**

### 2.3 如果 `novel-workflow/` 子目录已存在

- 先 `ls docs/m7_artifacts/skill_backup_20260418/novel-workflow/` 查看内容
- **如果为空**:直接用该空目录接收后续 mv(跳过 §3.1 step1)
- **如果已有内容且与顶层 4 文件/templates 不重名**:照常 mv
- **如果已有同名文件**:立即停止,报 "CONFLICT: novel-workflow/X 已存在,请作者处理",不要强制覆盖

---

## 3. 执行步骤(严格按顺序)

### 3.1 Step 1 — 新建子目录

```bash
mkdir docs/m7_artifacts/skill_backup_20260418/novel-workflow
```

验证:
```bash
test -d docs/m7_artifacts/skill_backup_20260418/novel-workflow || echo "FAIL-STEP1: novel-workflow 子目录创建失败"
```

### 3.2 Step 2 — 移动 4 个散文件

```bash
cd docs/m7_artifacts/skill_backup_20260418/
mv REFACTOR.md novel-workflow/
mv SKILL.md novel-workflow/
mv SKILL.md.bak novel-workflow/
mv SPEC.md novel-workflow/
```

> **注意**:PowerShell 下用 `Move-Item`:
> ```powershell
> Set-Location docs/m7_artifacts/skill_backup_20260418/
> Move-Item REFACTOR.md novel-workflow/
> Move-Item SKILL.md novel-workflow/
> Move-Item SKILL.md.bak novel-workflow/
> Move-Item SPEC.md novel-workflow/
> ```

### 3.3 Step 3 — 移动 templates/ 目录

```bash
# bash
mv templates novel-workflow/

# 或 PowerShell
Move-Item templates novel-workflow/
```

---

## 4. 自检命令(执行完 §3 后必须跑,任一失败立即报错并停止)

```bash
cd D:/动画/众生界

# 自检 1:顶层恰好 8 个条目(全部是目录)
count=$(ls docs/m7_artifacts/skill_backup_20260418/ | wc -l)
[ "$count" -eq 8 ] || echo "FAIL-1: 顶层条目数=$count,应为 8"

# 自检 2:顶层无散文件(只有目录)
stray=$(find docs/m7_artifacts/skill_backup_20260418/ -maxdepth 1 -type f | wc -l)
[ "$stray" -eq 0 ] || echo "FAIL-2: 顶层仍有 $stray 个散文件"

# 自检 3:novel-workflow 子目录存在且包含 5 个条目(4 文件 + 1 templates 目录)
test -d docs/m7_artifacts/skill_backup_20260418/novel-workflow || echo "FAIL-3a: novel-workflow 子目录不存在"
nw_count=$(ls docs/m7_artifacts/skill_backup_20260418/novel-workflow/ | wc -l)
[ "$nw_count" -eq 5 ] || echo "FAIL-3b: novel-workflow 内条目数=$nw_count,应为 5"

# 自检 4:novel-workflow 必含 4 个预期文件 + templates 目录
for f in REFACTOR.md SKILL.md SKILL.md.bak SPEC.md; do
  test -f docs/m7_artifacts/skill_backup_20260418/novel-workflow/$f || echo "FAIL-4: 缺 novel-workflow/$f"
done
test -d docs/m7_artifacts/skill_backup_20260418/novel-workflow/templates || echo "FAIL-5: 缺 novel-workflow/templates/"

# 自检 5:8 个子目录名必须与项目 12 skill 中的对应 8 个吻合
expected="novel-workflow novelist-canglan novelist-evaluator novelist-jianchen novelist-moyan novelist-worldview-generator novelist-xuanyi novelist-yunxi"
actual=$(ls docs/m7_artifacts/skill_backup_20260418/ | sort | tr '\n' ' ' | sed 's/ $//')
expected_sorted=$(echo $expected | tr ' ' '\n' | sort | tr '\n' ' ' | sed 's/ $//')
[ "$actual" = "$expected_sorted" ] || echo "FAIL-6: 8 子目录名不匹配。实际=[$actual] 期望=[$expected_sorted]"

# 自检 6:pytest 不应被影响(此步纯文件移动)
# 不跑 pytest 节省时间,但确保项目结构未动
test -d core/inspiration || echo "FAIL-7: 项目结构被意外破坏"

echo "ALL CHECKS DONE — 若上方无 FAIL 字样则全部通过"
```

6 条全 pass → 报告"N18a 备份结构修复完成,顶层已恢复 8 子目录结构",等待 coffeeliuwei 审阅并推进 P0-2。

---

## 5. 完成判据

- `docs/m7_artifacts/skill_backup_20260418/` 顶层 = 8 个子目录,0 散文件
- `novel-workflow/` 子目录含 `REFACTOR.md / SKILL.md / SKILL.md.bak / SPEC.md / templates/`
- 其他 7 个 skill 子目录内容**未被动过**
- `~/.agents/skills/` **未被触碰**
- **无 git commit**
- 本仓库 `git status` 只应显示 `docs/m7_artifacts/skill_backup_20260418/` 内的重命名(rename)

---

## 6. 完成后更新 ROADMAP

完成后,opencode 或未来 Claude 须把 [ROADMAP_众生界v2实施_20260419.md](./ROADMAP_众生界v2实施_20260419.md) §3.1 P0-1 行末的 🔴 改为 ✅,并在 §5 时间线追加一行:

```
| 2026-04-19 | opencode | N18a 备份结构修复完成 | skill_backup_20260418/ 恢复 8 子目录 |
```

下一步:启动 **P0-2 N18b n18_test_log.txt 补建**(计划由未来 Claude 按 ROADMAP §3.1 补写)。

---

**计划结束。opencode 按 §3 → §4 顺序执行。**
