# M3 · P1-β · 查询接入 + sync_cases 反转薄壳化

> **创建时间**：2026-04-18 03:15（上海时间）
> **里程碑编号**：主线路线图 **M3**
> **前置条件**：M2-α/β 已完成 ✅
> **后置触发**：opencode 完成此计划后 → 通知 Claude 写 **M4**（P2 · fixture 补齐 + intent_classifier 字符串外化）
> **执行模型**：opencode (GLM5)。所有步骤含完整代码块和精确锚点
> **预估工时**：~2 小时（M4 接入 ~1h + N5 反转 ~30min + 回归 ~30min）

---

## 0. 关键发现（必读，决定本计划走向）

### 发现 1：N5 路线图前提是错的

路线图 §2 写的是"sync_to_qdrant.py 与 sync_manager.py 是两份重复实现，保留后者"。逐行对比后事实正相反：

| 维度 | `.case-library/scripts/sync_to_qdrant.py`（482 行）| `modules/knowledge_base/sync_manager.sync_cases()`（117 行）|
|------|---------------------------|-----------------------------------|
| Collection | `case_library_v2`（生产 256k 点的真实集合）| `case_library`（与 `core/config_loader.py:50` 配置 `case_library → case_library_v2` 不一致，**bug**）|
| 断点续传 | ✅ scroll 取已存 case_id | ❌ 无 |
| Point ID | `uuid5(case_id)` 确定性 | 自增 `i`（重跑必冲突）|
| Payload 字段 | 11 个（含 techniques/keywords/writers/emotion_value/content_preview）| 7 个（缺 4 个关键字段）|
| 文件发现 | `rglob("*.json")` 递归 | `iterdir + glob` 仅一级 |

**结论**：sync_to_qdrant.py 是生产级实现，sync_manager.sync_cases() 是早期未成熟版本。

**用户已批准方案 A**（2026-04-18 对话记录）：**反转薄壳方向**——保留 sync_to_qdrant.py 的全部逻辑，把 sync_manager.sync_cases() 改为通过 `importlib.util` 加载并委托给 sync_to_qdrant.py 的 `QdrantSyncer`。

### 发现 2：sync_cases 的 5 处生产调用方

不能删除 `sync_cases` 方法本身，必须保留方法签名、返回 int (synced count)：

- `core/change_detector/sync_manager_adapter.py:222`
- `modules/knowledge_base/__init__.py:163` (KB facade)
- `tools/scene_discoverer.py:602`
- `tools/data_migrator.py:678, 876`
- `core/cli.py:195` (走 `kb.sync(target="case")` → `sync_manager.sync(target="case")` → `sync_cases`)

### 发现 3：M4 的 4 个 QUERY 意图

`core/conversation/intent_classifier.py` 中归到 `IntentCategory.QUERY` 的意图共 4 个：

| 意图 | 实体 | 是否需要向量检索 |
|------|------|-----------------|
| `query_character` | `character_name` | ✅ 需要——走 `UnifiedRetrievalAPI.search_novel(query=name, entity_type="角色")` |
| `query_progress` | 无 | ❌ 走 `WorkflowStateChecker`（已注入为 `self.workflow_checker`）|
| `inspiration_status_query` | 无 | ❌ 待 M5 后接入 inspiration_engine 状态，此处给信息性 stub |
| `constraint_query` | 无 | ❌ 待后续 constraint_store 暴露 API，此处给信息性 stub |

`_execute_query` 需按 `intent_result.intent` 名分支，而非一刀切。

### 发现 4：CASE_COLLECTION 类属性需修正

`modules/knowledge_base/sync_manager.py:84` 写死 `CASE_COLLECTION = "case_library"`，与 `core/config_loader.py` 的映射 `case_library → case_library_v2` 不一致。`get_sync_status()` 因此一直查空集合。本计划顺手修。

> **同类 bug 备注**：`NOVEL_COLLECTION = "novel_settings"` / `TECHNIQUE_COLLECTION = "writing_techniques"` 也与配置不符（应为 `_v2`），但**不在 M3 范围**——动它们影响 `sync_novel_settings` / `sync_techniques` 的现有行为，需要单独评估。本计划只修 CASE_COLLECTION。

---

## 1. 步骤 1：建立测试基线

```bash
cd /d/动画/众生界
python -m pytest tests/ --tb=no -q 2>&1 | tail -5
```

**记录**到本计划末尾"步骤 1 基线"小节，格式：
```
基线（M2-β 完成后，M3 开始前）：
- collected: <数字>
- passed: <数字>
- failed: <数字>
- 时间戳: <UTC+8>
```

---

## 2. Part A · M4 `_execute_query` 接入真实检索

### 2.1 在 `ConversationEntryLayer.__init__` 末尾添加 lazy 字段

**目标文件**：`core/conversation/conversation_entry_layer.py`
**精确锚点**：定位字符串 `# 复用同一 IntentRouter 实例以保持多轮对话状态`（在 line 107 附近）

**操作**：在该注释行**之前**（即 `self._conversation_context: List[Dict[str, Any]] = []` 这行**之后**）插入空行 + 下方 2 行：

```python
        # [M3-α] 检索 API 懒加载，避免启动时加载 BGE-M3 模型（数百 MB）
        self._retrieval_api = None
```

> **说明**：插入位置在第 105-107 行之间。务必保持 8 空格缩进（类内方法体）。

**校验**：
```bash
grep -n "self._retrieval_api = None" core/conversation/conversation_entry_layer.py
# 期望：1 行命中
```

### 2.2 在 `ConversationEntryLayer` 类内添加 `_get_retrieval_api` 辅助方法

**目标文件**：同上
**精确锚点**：定位字符串 `def _execute_query(`（约 line 517）

**操作**：在 `def _execute_query(` **之前**插入下方完整方法（保持 4 空格类内缩进）：

```python
    def _get_retrieval_api(self):
        """[M3-α] 懒加载 UnifiedRetrievalAPI（首次调用时初始化 BGE-M3）"""
        if self._retrieval_api is None:
            from core.retrieval.unified_retrieval_api import UnifiedRetrievalAPI

            self._retrieval_api = UnifiedRetrievalAPI()
        return self._retrieval_api

```

**校验**：
```bash
grep -n "_get_retrieval_api" core/conversation/conversation_entry_layer.py
# 期望：2 行命中（定义 + 调用，调用在 2.3 步骤后出现）
```

### 2.3 替换 `_execute_query` 方法体

**目标文件**：同上
**精确锚点 - 旧代码**（line 517-528 整段）：

```python
    def _execute_query(
        self, intent_result: IntentResult, user_input: str
    ) -> ProcessingResult:
        """执行查询"""
        # 这里简化实现，实际需要调用检索系统
        return ProcessingResult(
            status=ProcessingStatus.SUCCESS,
            intent=intent_result.intent,
            entities=intent_result.entities,
            message=f"查询意图：{intent_result.intent}\n实体：{intent_result.entities}",
            data={"query": intent_result.entities},
        )
```

**新代码**（精确替换，保持原缩进）：

```python
    def _execute_query(
        self, intent_result: IntentResult, user_input: str
    ) -> ProcessingResult:
        """[M3-α] 按意图分支接入真实检索 / 状态查询"""
        intent = intent_result.intent
        entities = intent_result.entities

        # 分支 1：角色设定查询 → 走向量检索
        if intent == "query_character":
            character_name = entities.get("character_name", "").strip()
            if not character_name:
                return ProcessingResult(
                    status=ProcessingStatus.MISSING_INFO,
                    intent=intent,
                    entities=entities,
                    message="请告诉我要查询哪个角色（例如：查一下赵恒的设定）",
                )
            try:
                api = self._get_retrieval_api()
                hits = api.search_novel(
                    query=character_name, entity_type="角色", top_k=5
                )
            except Exception as e:
                return ProcessingResult(
                    status=ProcessingStatus.ERROR,
                    intent=intent,
                    entities=entities,
                    message=f"检索系统暂时不可用：{e}",
                )
            if not hits:
                return ProcessingResult(
                    status=ProcessingStatus.SUCCESS,
                    intent=intent,
                    entities=entities,
                    message=f"未找到「{character_name}」的设定记录。",
                    data={"query": character_name, "hits": []},
                )
            lines = [f"找到 {len(hits)} 条与「{character_name}」相关的设定："]
            for i, h in enumerate(hits, 1):
                name = h.get("name", h.get("payload", {}).get("name", "未命名"))
                desc = h.get("description", h.get("payload", {}).get("description", ""))
                score = h.get("score", 0)
                lines.append(f"{i}. {name}（相似度 {score:.2f}）：{desc[:80]}")
            return ProcessingResult(
                status=ProcessingStatus.SUCCESS,
                intent=intent,
                entities=entities,
                message="\n".join(lines),
                data={"query": character_name, "hits": hits},
            )

        # 分支 2：进度查询 → 走 WorkflowStateChecker
        if intent == "query_progress":
            try:
                state = self.workflow_checker.check_state()
            except Exception as e:
                return ProcessingResult(
                    status=ProcessingStatus.ERROR,
                    intent=intent,
                    entities=entities,
                    message=f"无法读取工作流状态：{e}",
                )
            return ProcessingResult(
                status=ProcessingStatus.SUCCESS,
                intent=intent,
                entities=entities,
                message=f"当前工作流状态：{state}",
                data={"workflow_state": state},
            )

        # 分支 3：灵感引擎状态查询 → 暂未接入（M5 后实现）
        if intent == "inspiration_status_query":
            return ProcessingResult(
                status=ProcessingStatus.SUCCESS,
                intent=intent,
                entities=entities,
                message=(
                    "灵感引擎状态查询接口尚未接入（计划 M5 端到端验证后实现）。"
                    "目前可手动查看 data/inspiration_state.json。"
                ),
                data={"not_implemented": True},
            )

        # 分支 4：约束查询 → 暂未接入（待 constraint_store 暴露读取 API）
        if intent == "constraint_query":
            return ProcessingResult(
                status=ProcessingStatus.SUCCESS,
                intent=intent,
                entities=entities,
                message=(
                    "约束查询接口尚未接入（待 constraint_store 暴露读 API）。"
                    "目前可手动查看 data/constraint_store.json。"
                ),
                data={"not_implemented": True},
            )

        # 分支 5：未知 QUERY 意图 → 多源 fallback 检索
        try:
            api = self._get_retrieval_api()
            results = api.retrieve(
                query=user_input,
                sources=["novel", "technique", "case"],
                top_k=3,
            )
        except Exception as e:
            return ProcessingResult(
                status=ProcessingStatus.ERROR,
                intent=intent,
                entities=entities,
                message=f"通用检索失败：{e}",
            )
        total = sum(len(v) for v in results.values())
        return ProcessingResult(
            status=ProcessingStatus.SUCCESS,
            intent=intent,
            entities=entities,
            message=f"通用检索命中 {total} 条结果（设定/技法/案例 各前 3）",
            data={"query": user_input, "results": results},
        )
```

**校验**：
```bash
grep -c "intent ==" core/conversation/conversation_entry_layer.py
# 期望：≥4（4 个分支判断）

python -c "from core.conversation.conversation_entry_layer import ConversationEntryLayer; print('import OK')"
# 期望：输出 import OK（不会因 lazy 加载触发 BGE-M3 加载）
```

---

## 3. Part B · N5 · `sync_cases` 反转薄壳化

### 3.1 修正 `CASE_COLLECTION` 类常量

**目标文件**：`modules/knowledge_base/sync_manager.py`
**精确锚点 - 旧代码**（line 84）：

```python
    CASE_COLLECTION = "case_library"
```

**新代码**（精确替换）：

```python
    CASE_COLLECTION = "case_library_v2"  # [M3-β] 与 core/config_loader.py 配置一致，对齐生产 Qdrant 集合
```

> **不要**改 `NOVEL_COLLECTION` 和 `TECHNIQUE_COLLECTION`——同类 bug 但不在 M3 范围（详见 §0 发现 4 备注）。

### 3.2 替换 `sync_cases` 方法体为薄壳

**目标文件**：同上
**精确锚点 - 旧代码**（line 471-587 整个 `def sync_cases` 方法，从 `def sync_cases(self, rebuild: bool = False) -> int:` 起，到 `return len(points)` 止——见原文件确认结尾在 line 587 附近）

**操作**：把整个 `sync_cases` 方法体（约 117 行）替换为下方薄壳实现：

```python
    def sync_cases(self, rebuild: bool = False) -> int:
        """[M3-β] 薄壳：委托给 .case-library/scripts/sync_to_qdrant.py 的 QdrantSyncer

        反转方向说明：
        - sync_to_qdrant.py 是 case_library_v2 (256k 点) 的生产级实现
          含断点续传、uuid5 确定性 ID、11 字段 payload、rglob 递归发现
        - 本方法原始实现是早期未成熟版本（无 resume、自增 ID、payload 字段缺失）
        - 用户决议（2026-04-18 方案 A）：保留 sync_to_qdrant 全部逻辑，
          本方法改为通过 importlib 加载并委托

        Args:
            rebuild: True 时先删除集合再全量同步；False 时启用断点续传
        Returns:
            成功同步的案例数量
        """
        import importlib.util

        script_path = (
            self.project_dir / ".case-library" / "scripts" / "sync_to_qdrant.py"
        )
        if not script_path.exists():
            print(f"  [错误] sync_to_qdrant.py 不存在: {script_path}")
            return 0

        # rebuild=True 语义：删除集合后重新同步
        if rebuild:
            try:
                client = self._get_client()
                collections = [
                    c.name for c in client.get_collections().collections
                ]
                if self.CASE_COLLECTION in collections:
                    print(f"  [M3-β rebuild] 删除旧集合 {self.CASE_COLLECTION}")
                    client.delete_collection(collection_name=self.CASE_COLLECTION)
            except Exception as e:
                print(f"  [警告] rebuild 删除集合失败（继续尝试同步）: {e}")

        # 显式按文件路径加载，避免与 .novel-extractor/sync_to_qdrant.py 模块名冲突
        spec = importlib.util.spec_from_file_location(
            "_case_lib_sync_to_qdrant", str(script_path)
        )
        if spec is None or spec.loader is None:
            print(f"  [错误] 无法加载 sync_to_qdrant.py: {script_path}")
            return 0

        module = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(module)

        syncer = module.QdrantSyncer(use_docker=self.use_docker)
        result = syncer.sync(resume=not rebuild)
        return int(result.get("synced", 0))
```

**校验**：
```bash
grep -n "M3-β" modules/knowledge_base/sync_manager.py
# 期望：≥3 行命中（CASE_COLLECTION 注释 + sync_cases docstring + rebuild 日志）

# 字面行数对比：替换后 sync_cases 应远少于原 117 行
sed -n '/def sync_cases/,/def _extract_technique_sections/p' modules/knowledge_base/sync_manager.py | wc -l
# 期望：约 50-60 行（薄壳 + docstring）

# 静态导入测试
python -c "from modules.knowledge_base.sync_manager import SyncManager; print('OK')"
# 期望：输出 OK
```

> **不要做**实际同步运行验证——sync_cases 一跑要数分钟到小时，且会改 Qdrant 状态。本步只验证语法和导入。

---

## 4. 步骤 4：回归测试，对比基线

```bash
python -m pytest tests/ --tb=short -q 2>&1 | tail -15
```

**判定规则**：
- ✅ 通过：`passed` 数量 ≥ 步骤 1 基线 passed
- ✅ 通过：`failed` 数量 ≤ 基线 failed
- ❌ 失败：新增任何 ImportError 含 `unified_retrieval_api`、`sync_to_qdrant`、`UnifiedRetrievalAPI` 字样
- ❌ 失败：新增非 import 类失败

**如果失败**：把失败 trace 完整贴到本计划末尾"步骤 4 失败记录"小节，**停下，不 commit，不继续**，等用户处理。

---

## 5. 步骤 5：grep 兜底确认

```bash
echo "=== A. _execute_query 不再是空壳（应有 4 个 if intent == 分支）==="
grep -c "if intent ==\|if intent_result.intent ==" core/conversation/conversation_entry_layer.py

echo ""
echo "=== B. sync_cases 已薄壳化（应包含 importlib + sync_to_qdrant 引用）==="
grep -n "importlib.util\|sync_to_qdrant" modules/knowledge_base/sync_manager.py

echo ""
echo "=== C. CASE_COLLECTION 已修正 ==="
grep -n "CASE_COLLECTION = " modules/knowledge_base/sync_manager.py
```

**期望**：
- A 段：≥4
- B 段：≥3 行命中
- C 段：1 行命中且值为 `"case_library_v2"`

---

## 6. 步骤 6：更新主线路线图

**目标文件**：`docs/计划_主线路线图_至闭源分发_20260418.md`

**操作 1**：找到表格 §1 中 M3 那一行：
```
| **M3** | P1-β · M4 `_execute_query` 接入 + N5 重复 sync 脚本合并 | ⏳ 待 Claude 写子计划 | **待写** → 路径预定：`docs/计划_P1β_查询接入与脚本合并_20260418.md` | `_execute_query` 调真实检索 + sync_to_qdrant.py 改为薄壳调 sync_manager |
```

**替换为**：
```
| **M3** | P1-β · M4 `_execute_query` 接入 + N5 sync 脚本合并（方案 A 反转）| ✅ 已完成 | [`docs/计划_P1β_查询接入与脚本合并_20260418.md`](计划_P1β_查询接入与脚本合并_20260418.md) | `_execute_query` 按 4 个 QUERY 意图分支接入 UnifiedRetrievalAPI + sync_manager.sync_cases() 改为委托 sync_to_qdrant.py + CASE_COLLECTION 修正为 `case_library_v2` |
```

**操作 2**：找到 §5 状态总表中 M2/M3 行：
```
M2 [✅] P1-α 旧框架合并（拆 α/β）                    — α/β 全部完成
M3 [⏳] P1-β M4 接入 + N5 合并                      — 子计划待 Claude 写 ← 当前
```

**替换为**：
```
M2 [✅] P1-α 旧框架合并（拆 α/β）                    — α/β 全部完成
M3 [✅] P1-β M4 接入 + N5 合并（方案 A 反转）          — 完成
M4 [⏳] P2 fixture + 字符串外化                     — 子计划待 Claude 写 ← 当前
```

> 注意：原 §5 还有一行 `M4 [⏳] P2 fixture + 字符串外化 — 子计划待写`，该行已存在，**不要重复添加**——只是把"← 当前"标识从 M3 移到 M4。

**操作 3**：找到"**当前指针**"那行：
```
**当前指针**：M3（opencode 待 Claude 写完 M3 子计划后开始执行）
```

**替换为**：
```
**当前指针**：M4（opencode 待 Claude 写完 M4 子计划后开始执行）
```

---

## 7. 完成验收

opencode 完成全部步骤后贴出以下输出：

```bash
# A. M4 _execute_query 已接入
grep -c "if intent ==\|if intent_result.intent ==" core/conversation/conversation_entry_layer.py
grep -n "_get_retrieval_api" core/conversation/conversation_entry_layer.py

# B. N5 sync_cases 已薄壳化
grep -n "M3-β\|importlib.util\|CASE_COLLECTION = " modules/knowledge_base/sync_manager.py

# C. 静态导入双验证
python -c "from core.conversation.conversation_entry_layer import ConversationEntryLayer; print('Conv OK')"
python -c "from modules.knowledge_base.sync_manager import SyncManager; print('Sync OK')"

# D. 步骤 4 测试结果（再跑一遍）
python -m pytest tests/ --tb=no -q 2>&1 | tail -5

# E. 路线图状态
grep -E "M3.*✅|当前指针" docs/计划_主线路线图_至闭源分发_20260418.md | head -5

# F. 工作树状态
git status --short | head -25
```

---

## 8. 不要做的事

- ❌ **不要** `git commit`——等用户审完所有 M1+M2-α+M2-β+M3 改动后统一提交
- ❌ **不要**实际跑 `sync_manager.sync_cases()` 做集成验证——会改 Qdrant 状态、耗时数分钟到小时；语法 + 导入测试足够
- ❌ **不要**修改 `NOVEL_COLLECTION` / `TECHNIQUE_COLLECTION`——同类 bug 但不在 M3 范围
- ❌ **不要**删除 `sync_cases` 方法本身——5 处生产代码依赖此方法签名（详见 §0 发现 2）
- ❌ **不要**把 `_execute_query` 的 4 个分支合并成一个 `api.retrieve()` 调用——`query_progress` 等不需要向量检索
- ❌ **不要**在 `__init__` 里立刻初始化 `UnifiedRetrievalAPI`——必须 lazy（启动时加载 BGE-M3 数百 MB 不可接受）
- ❌ **不要**自己写 M4 子计划——等 Claude 写
- ❌ 步骤 4 失败时**不要**自己尝试修复——停下，写到失败记录小节

---

## 步骤 1 基线
（opencode 在执行步骤 1 后填写）

```
基线（M2-β 完成后，M3 开始前）：
- collected: 454
- pytest 可正常收集测试
- 时间戳: 2026-04-18 (UTC+8)
- 注：pytest 运行时无输出，可能因 Windows PowerShell 管道问题
```

---

## 步骤 4 失败记录
（仅在测试回归失败时填写）

```
（默认为空）
```
