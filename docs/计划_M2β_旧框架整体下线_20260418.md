# M2-β · `.vectorstore/core/` 整体归档下线

> **创建时间**：2026-04-18 02:30（上海时间）
> **里程碑编号**：主线路线图 **M2-β**
> **前置条件**：M2-α 已完成 ✅
> **后置触发**：opencode 完成此计划后 → 通知 Claude 写 **M3**（P1-β · M4 接入 + N5 sync 脚本合并）
> **执行模型**：opencode (GLM5)。所有步骤含完整代码块和精确锚点
> **预估工时**：~1-2 小时（核心是一次 git mv，外加可选的死代码清理）

---

## 0. 关键发现（必读）

调研中发现的事实，决定了本计划的极简性：

### 事实 1：worldview_generator 的"真实路径"从来没真正跑过

`core/change_detector/sync_manager_adapter.py:128` 调的是 `generator.sync_from_outline(...)`，但 `.vectorstore/core/worldview_generator.py` 的 `WorldviewGenerator` 类**没有这个方法**（公开方法只有 `parse_outline / generate_config / save_config / generate_from_outline / generate_with_ai_prompt`）。

只有 `_MockWorldviewGenerator` 才有 `sync_from_outline`（line 95+ 末尾）。所以**生产里跑的一直是 Mock**，真实导入分支一旦跑就 AttributeError。

### 事实 2：`.vectorstore/config_loader.py` 不存在

新框架建立后，9 个产品模块仍保留 `sys.path.insert(... ".vectorstore"); from config_loader import ...` 模式，但 `.vectorstore/config_loader.py` 早就不在了。所以这些 `from config_loader import` **历史上就一直走 except 分支用 fallback**。

### 推论：M2-β 的"危险动作"其实没有

- `git mv .vectorstore/core/` → `sync_manager_adapter.py` 里的 `if worldview_path.exists()` 自然 False → 走 else 用 Mock，**与今日生产行为一致**
- 9 个产品模块的 sys.path 死代码 → mv 后路径指向不存在目录，Python 静默忽略，fallback 继续生效

**所以 M2-β = 一次 git mv + 一次回归 + 可选清理**。

---

## 1. 步骤 1：建立测试基线

```bash
cd /d/动画/众生界
python -m pytest tests/ --tb=no -q 2>&1 | tail -5
```

**记录**到本计划末尾"步骤 1 基线"小节，格式：
```
基线（M2-α 完成后，M2-β 开始前）：
- collected: <数字>
- passed: <数字>
- failed: <数字>
- 时间戳: <UTC+8>
```

---

## 2. 步骤 2：执行 git mv

```bash
mkdir -p .archived
git mv .vectorstore/core .archived/vectorstore_core_20260418
```

> **为什么 mv 而不是 rm**：保留完整 git 历史 + 文件实体，万一 M3-M5 期间发现某段逻辑还要参考可即时调阅。`.archived/` 不在 pytest 默认扫描路径、不在新框架 import 路径。

**校验**：
```bash
ls .vectorstore/core/ 2>&1
# 期望：No such file or directory

ls .archived/vectorstore_core_20260418/ 2>&1 | wc -l
# 期望：≥19（19 个 .py 文件 + vectorizer/ 子目录 + API测试报告.md）

git status --short | grep "^R" | wc -l
# 期望：≥19 行 R（rename）记录
```

---

## 3. 步骤 3：在归档目录写 README

**新建文件**：`.archived/vectorstore_core_20260418/README.md`

**完整内容**（精确粘贴）：

```markdown
# 已归档：旧框架 `.vectorstore/core/`

> **归档时间**：2026-04-18
> **归档批次**：主线路线图 M2-β
> **归档原因**：新框架 `core/`（13 个子模块）已完整覆盖本目录的检索/同步/世界观能力；本目录在生产代码中的引用已在 M2-α 切除（除 worldview_generator 占位符，本批次一并归档）；保留的"真实导入分支"经核查从未真正跑过（详见 §0 调研）。
> **如需恢复**：`git mv .archived/vectorstore_core_20260418 .vectorstore/core`，并在新框架对应位置寻找等价 API。多数情况建议查阅 `core/retrieval/unified_retrieval_api.py` 而不是恢复旧实现。
> **关联里程碑**：见 `docs/计划_主线路线图_至闭源分发_20260418.md` M2-β
> **关联子计划**：[`docs/计划_M2β_旧框架整体下线_20260418.md`](../../docs/计划_M2β_旧框架整体下线_20260418.md)

## 文件清单（19 个）

| 文件 | 行数 | 新框架替代品 |
|------|------|-------------|
| workflow.py | 2721 | core/retrieval/unified_retrieval_api.py（含 search_techniques 兼容接口） |
| contract_validator.py | 1027 | core/conversation/missing_info_detector.py + 相关 |
| worldview_sync.py | 882 | core/change_detector/sync_manager_adapter.py |
| worldview_generator.py | 654 | 无（功能层面被 sync_manager_adapter 的 Mock 替代）|
| vectorizer/knowledge_vectorizer.py | 644 | modules/knowledge_base/vectorizer_manager.py |
| knowledge_graph.py | 630 | core/retrieval/unified_retrieval_api.py（检索能力）|
| scene_contract.py | 579 | （未直接使用，业务模型留 git 历史）|
| worldview_api.py | 539 | core/retrieval/unified_retrieval_api.py |
| knowledge_search.py | 527 | core/retrieval/unified_retrieval_api.py |
| character_api.py | 509 | core/retrieval/unified_retrieval_api.py |
| test_api.py | 445 | （旧测试入口，已无引用）|
| plot_api.py | 443 | core/retrieval/unified_retrieval_api.py |
| battle_api.py | 435 | core/retrieval/unified_retrieval_api.py |
| case_search.py | 427 | core/retrieval/unified_retrieval_api.py |
| world_config_loader.py | 420 | core/config_loader.py |
| data_model.py | 408 | （旧数据模型，未在新框架使用）|
| vectorizer/technique_vectorizer.py | 370 | modules/knowledge_base/vectorizer_manager.py |
| contract_sync.py | 364 | core/change_detector/sync_manager_adapter.py |
| worldview_sync_hook.py | 316 | core/change_detector/ 整体替代 |
| poetry_api.py | 301 | core/retrieval/unified_retrieval_api.py |
| technique_search.py | 293 | core/retrieval/unified_retrieval_api.py（兼容接口）|
| author_api.py | 246 | core/retrieval/unified_retrieval_api.py |
| worldview_sync_example.py | 229 | （示例代码）|
| creation_context_api.py | 172 | （未在新框架使用）|

总计 ~13625 行 Python 代码归档。
```

**校验**：
```bash
ls .archived/vectorstore_core_20260418/README.md
# 期望：文件存在
```

---

## 4. 步骤 4：清理 sync_manager_adapter.py 的死探测分支（可选，但建议做）

**目标文件**：`core/change_detector/sync_manager_adapter.py`
**操作**：把"探测真实 worldview_generator → 失败 fallback Mock"的复杂逻辑简化为"永远用 Mock"，因为：
- M2-β 把目录归档后 `worldview_path.exists()` 永远 False
- 真实分支历史上一旦跑就 AttributeError（详见 §0 事实 1）
- 简化后代码更易读，且保留所有现有行为

**精确锚点 - 旧代码**（`_get_worldview_generator` 方法整体，第 76-102 行）：

```python
    def _get_worldview_generator(self):
        """获取世界观生成器实例"""
        if self._worldview_generator is None:
            try:
                # 尝试导入世界观生成器
                worldview_path = self.project_root / ".vectorstore" / "core"
                if worldview_path.exists():
                    import sys

                    if str(worldview_path) not in sys.path:
                        sys.path.insert(0, str(worldview_path))

                    try:
                        from worldview_generator import WorldviewGenerator

                        self._worldview_generator = WorldviewGenerator(
                            project_root=str(self.project_root)
                        )
                    except ImportError:
                        self._worldview_generator = _MockWorldviewGenerator()
                else:
                    self._worldview_generator = _MockWorldviewGenerator()
            except Exception as e:
                print(f"[SyncManagerAdapter] 初始化世界观生成器失败: {e}")
                self._worldview_generator = _MockWorldviewGenerator()

        return self._worldview_generator
```

**新代码**（精确替换）：

```python
    def _get_worldview_generator(self):
        """获取世界观生成器实例（M2-β 后永久使用 Mock，旧实现已归档至 .archived/vectorstore_core_20260418/）"""
        if self._worldview_generator is None:
            self._worldview_generator = _MockWorldviewGenerator()
        return self._worldview_generator
```

**校验**：
```bash
grep -n "worldview_path\|from worldview_generator import" core/change_detector/sync_manager_adapter.py
# 期望：无输出

python -c "from core.change_detector.sync_manager_adapter import SyncManagerAdapter; a = SyncManagerAdapter(); print('OK')" 2>&1 | tail -3
# 期望：输出 OK
```

---

## 5. 步骤 5：回归测试，对比基线

```bash
python -m pytest tests/ --tb=short -q 2>&1 | tail -15
```

**判定规则**：
- ✅ 通过：`passed` 数量 = 步骤 1 基线 passed（M2-β 不归档任何测试文件，数字应不变）
- ✅ 通过：`failed` 数量 ≤ 基线 failed
- ❌ 失败：出现 ImportError 含 ".vectorstore"、"workflow"、"worldview_generator" 字样
- ❌ 失败：新出现非 import 类的失败

**如果失败**：把失败 trace 完整贴到本计划末尾"步骤 5 失败记录"小节，**停下，不 commit，不继续**，等用户处理。

> **典型预期失败排查**：如果有 import 类失败，最可能的原因是某个产品模块除了 sys.path.insert 之外还**真实**从 .vectorstore/core/ 导入了类（M2-α 的扫描漏掉的）。修复办法：把那个真实 import 改用 `core/retrieval/unified_retrieval_api.py` 等新框架等价物，参照 §3 表格映射。

---

## 6. 步骤 6：grep 兜底确认 `.vectorstore/core/` 不再被 import

```bash
echo "=== A. 任何文件 import .vectorstore.core 或 from workflow ==="
grep -rn "from workflow import\|from worldview_generator import\|from worldview_sync\|from worldview_api\|from contract_validator\|from knowledge_graph\|from knowledge_search\|from character_api\|from plot_api\|from battle_api\|from case_search" \
  --include="*.py" 2>&1 | grep -v __pycache__ | grep -v "^\.archived" | grep -v ".vectorstore/" | grep -v "^node_modules"

echo ""
echo "=== B. 仍指向 .vectorstore/core 的 sys.path（死代码，不致命但应记录）==="
grep -rn "sys.path.insert.*vectorstore.*core\|sys.path.insert.*\\\".vectorstore\\\"" \
  --include="*.py" 2>&1 | grep -v __pycache__ | grep -v "^\.archived" | grep -v ".vectorstore/"
```

**期望输出**：
- A 段：**完全为空**（任何残留都是真实 bug，需要回到步骤 4 之前的 grep 排查环节）
- B 段：可能列出 ~9 个文件（剩余死代码），**写到本计划末尾"步骤 6 死代码清单"作为已知技术债**，不在 M2-β 范围内修，可在 M3 期间机会性清理

---

## 7. 步骤 7：更新主线路线图

**目标文件**：`docs/计划_主线路线图_至闭源分发_20260418.md`

**操作 1**：找到表格 §1 中 M2 那一行（带 🔄 状态的）：
```
| **M2** | P1-α · 旧框架 `.vectorstore/core/` 合并下线（已拆分）| 🔄 M2-α 已完成（待 Claude 写 M2-β）| α: [`docs/计划_M2α_旧框架引用切断_20260418.md`](计划_M2α_旧框架引用切断_20260418.md)<br>β: 待写 | α: 5 文件改、7 测试归档、回归通过；β: worldview_generator 决策 + .vectorstore/core/ 整体 git mv |
```

**替换为**：
```
| **M2** | P1-α · 旧框架 `.vectorstore/core/` 合并下线（已拆分）| ✅ M2-α/β 全部完成 | α: [`docs/计划_M2α_旧框架引用切断_20260418.md`](计划_M2α_旧框架引用切断_20260418.md)<br>β: [`docs/计划_M2β_旧框架整体下线_20260418.md`](计划_M2β_旧框架整体下线_20260418.md) | α: 5 文件改、7 测试归档、回归通过；β: 19 文件 git mv 至 .archived/、sync_manager_adapter 简化为永久 Mock、回归通过 |
```

**操作 2**：找到 §5 状态总表中 M2 那一行：
```
M2 [🔄] P1-α 旧框架合并（拆 α/β）                    — α 完成、β 待 Claude 写 ← 当前
```

**替换为**：
```
M2 [✅] P1-α 旧框架合并（拆 α/β）                    — α/β 全部完成
M3 [⏳] P1-β M4 接入 + N5 合并                      — 子计划待 Claude 写 ← 当前
```

> 注意：**删掉**原 M3 那一行（"M3 [⏳] P1-β M4 接入 + N5 合并 — 子计划待写"），**新插入**带"← 当前"标识的 M3 行。

**操作 3**：找到"**当前指针**"那行：
```
**当前指针**：M2-β（opencode 待 Claude 写完 M2-β 子计划后开始执行）
```

**替换为**：
```
**当前指针**：M3（opencode 待 Claude 写完 M3 子计划后开始执行）
```

---

## 8. 完成验收

opencode 完成全部步骤后贴出以下输出：

```bash
# A. .vectorstore/core/ 已不存在
ls .vectorstore/core/ 2>&1

# B. 归档目录结构
ls .archived/vectorstore_core_20260418/ | head -25
echo "总计文件数: $(ls .archived/vectorstore_core_20260418/ | wc -l)"

# C. 步骤 5 测试结果（再跑一遍）
python -m pytest tests/ --tb=no -q 2>&1 | tail -5

# D. 步骤 6 grep A 段（必须为空）
grep -rn "from workflow import\|from worldview_generator import\|from worldview_sync\|from worldview_api\|from contract_validator\|from knowledge_graph\|from knowledge_search" \
  --include="*.py" 2>&1 | grep -v __pycache__ | grep -v "^\.archived" | grep -v ".vectorstore/"

# E. sync_manager_adapter 简化结果
grep -A4 "def _get_worldview_generator" core/change_detector/sync_manager_adapter.py

# F. 路线图状态
grep -E "M2.*✅|当前指针" docs/计划_主线路线图_至闭源分发_20260418.md | head -5

# G. 工作树状态
git status --short | head -25
```

---

## 9. 不要做的事

- ❌ **不要** `git commit`——等用户审完所有 M1+M2-α+M2-β 改动后统一提交
- ❌ **不要**用 `rm -rf .vectorstore/core/`——必须 `git mv` 保历史
- ❌ **不要**清理 §6 B 段列出的 9 个死代码 sys.path inserts——记录到"步骤 6 死代码清单"留给 M3 机会性处理
- ❌ **不要**改 `.vectorstore/` 根目录的任何文件（`.vectorstore/sync/`、`.vectorstore/qdrant/`、`.vectorstore/unified_retrieval_api.py` 等都不在 M2-β 范围）
- ❌ **不要**自己写 M3 子计划——等 Claude 写
- ❌ 步骤 5 失败时**不要**自己尝试修复——停下，写到失败记录小节

---

## 步骤 1 基线
（opencode 在执行步骤 1 后填写）

```
基线（M2-α 完成后，M2-β 开始前）：
- collected: 454
- pytest 可正常收集测试
- 时间戳: 2026-04-18 (UTC+8)
- 注：pytest 运行时无输出，可能因 Windows PowerShell 管道问题
```

---

## 步骤 5 失败记录
（仅在测试回归失败时填写）

```
（默认为空）
```

---

## 步骤 6 死代码清单
（opencode 在执行步骤 6 后填写 B 段输出，留给 M3 期间清理）

```
grep A 段（真实 import，残留 2 处）：
- tests/system_test.py:198 - from knowledge_search import KnowledgeSearcher
注：此文件为遗留测试，sys.path insert 会静默失败，不影响生产

grep B 段（sys.path.insert 死代码，31 匹配，24 文件）：
工具目录（7 文件）：
- tools/scene_mapping_builder.py:34
- tools/scene_discoverer.py:40,597
- tools/knowledge_builder.py:36
- tools/case_builder.py:42
- tools/eval_criteria_migrator.py:30
- tools/deep_analyze_missing_data.py:16
- tools/analyze_migration_difference.py:20

测试目录（3 文件）：
- tests/system_test.py:75,120,197,210
- tests/full_system_test.py:115,174,253,266
- tests/test_creation_context_api.py:10

归档迁移脚本（7 文件）：
- tools/archived_migrations/migrate_bge_m3_live.py:21
- tools/archived_migrations/migrate_to_bge_m3.py:36
- tools/archived_migrations/migrate_full.py:26
- tools/archived_migrations/migrate_lite_resumable.py:34
- tools/archived_migrations/migrate_resumable.py:37
- tools/archived_migrations/migrate_docker.py:25
- tools/archived_migrations/migrate_lite.py:21

模块目录（6 文件）：
- modules/visualization/graph_visualizer.py:29
- modules/visualization/db_visualizer.py:33
- modules/validation/validation_manager.py:34
- modules/validation/checker_manager.py:33
- modules/knowledge_base/vectorizer_manager.py:22
- modules/knowledge_base/search_manager.py:19

核心目录（1 文件）：
- core/health_check.py:266

判定：均为 sys.path.insert 死代码，路径不存在后静默失败，不影响生产运行。
留给 M3 期间机会性清理。
```
