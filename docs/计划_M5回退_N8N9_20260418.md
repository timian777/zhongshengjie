# M5 回退修复 · N8 (链路 D) + N9 (链路 C) + 复测

**创建时间**：2026-04-18 15:04 (UTC+8)
**作者**：Claude（按 M5 §6 失败回退矩阵 + 用户选 b）
**执行方**：opencode (GLM5)
**前置**：M5 已跑出证据，C ❌ / D ❌（见 `docs/m5_evidence/`）
**后置**：本计划全绿后 → Claude 写 M6 闭源/Cython 子计划

---

## §0 关键发现（必读，不要跳过）

### 0.1 链路 D 真相（不是 N1 调查任务）

`modules/knowledge_base/sync_manager.py:43` 的 `raise ImportError` **是 except 分支的占位**，不是真正的代码 bug。真正的 bug 在 `:16` 的 sys.path 设置：

```python
# sync_manager.py:16
_project_root = Path(__file__).resolve().parent.parent
# __file__ = modules/knowledge_base/sync_manager.py
# .parent       = modules/knowledge_base/
# .parent.parent = modules/                   ← 错！应该是项目根
# 应是 .parent.parent.parent = D:\动画\众生界\（包含 core/）
```

**且 config_loader 不在项目根，在 `core/config_loader.py`**。所以即便 path 正确也找不到——应改成 `from core.config_loader import (...)`，与同目录 `__init__.py:25`、`hybrid_search_manager.py:21`、`hybrid_sync_manager.py:21`、`hybrid_search_manager_lite.py:24` 的写法对齐（这些都是已验证可用的）。

**验证命令（在执行前先跑确认问题存在）**：

```bash
python -c "import sys; sys.path.insert(0,'.'); from modules.knowledge_base.sync_manager import SyncManager; print('OK')"
# 当前应输出 ImportError；修复后应输出 OK
```

### 0.2 链路 C 真相（Mock 签名其实是对的）

`core/change_detector/sync_manager_adapter.py:435` 的 Mock 签名 `sync_from_outline(self, outline_path: str)` **本身正确**，且生产路径 `:107` 调用时也传了参数：

```python
# :107
result = generator.sync_from_outline(str(outline_file))
```

错的是 **M5 §3.3 链路 C 测试驱动代码**没传参数。但按用户选 b："顺手修"防御性改造——**让 outline_path 可选并默认 '总大纲.md'**，未来再有人裸调也不炸。生产路径行为不变。

### 0.3 范围严格限定

- **不要**改 `_project_root` 计算逻辑以外的 sys.path 行为；
- **不要**动 `sync_manager.py:179` 的 `from core.config_loader import get_model_path`（已对）；
- **不要**改 `sync_from_outline` 的生产调用方（`:107` 一处，传参依然传）；
- **不要**改 M5 计划文档（已归档证据，不回写）；
- **不要**自行决定 M5 是否"通过"——本计划全绿后由 Claude 决定是否进 M6。

---

## §1 N8 · 修复 sync_manager.py 配置导入（链路 D）

### 1.1 文件锚点

`modules/knowledge_base/sync_manager.py`，第 12-62 行（整段 import 块 + 错误占位 fallback）。

### 1.2 当前代码（要被替换的精确块）

```python
# 从配置加载器导入路径获取函数
import sys
from pathlib import Path

_project_root = Path(__file__).resolve().parent.parent
import sys as _sys  # alias to avoid shadowing

if (
    str(_project_root) not in _sys.modules
    and str(_project_root) not in __import__("sys").path
):
    __import__("sys").path.insert(0, str(_project_root))

try:
    from config_loader import (
        get_project_root,
        get_qdrant_url,
        get_vectorstore_dir,
        get_qdrant_storage_dir,
        get_knowledge_graph_path,
        get_techniques_dir,
        get_case_library_dir,
    )

    HAS_CONFIG_LOADER = True
except Exception:
    # 如果无法加载配置加载器，后续逻辑将回退到默认路径
    HAS_CONFIG_LOADER = False

    # 仍保留名称以免后续代码引用出错
    def get_project_root():
        raise ImportError

    def get_vectorstore_dir():
        raise ImportError

    def get_qdrant_storage_dir():
        raise ImportError

    def get_qdrant_url():
        raise ImportError

    def get_knowledge_graph_path():
        raise ImportError

    def get_techniques_dir():
        raise ImportError

    def get_case_library_dir():
        raise ImportError
```

### 1.3 替换为（精确块，整段替换）

```python
# 从配置加载器导入路径获取函数
# [N8 2026-04-18] 修复：原 sys.path 计算 .parent.parent 指到 modules/ 而非项目根，
# 且 config_loader 在 core/ 下而非项目根；改用与 __init__.py:25、
# hybrid_search_manager.py:21 一致的 `from core.config_loader import (...)`。
try:
    from core.config_loader import (
        get_project_root,
        get_qdrant_url,
        get_vectorstore_dir,
        get_qdrant_storage_dir,
        get_knowledge_graph_path,
        get_techniques_dir,
        get_case_library_dir,
    )

    HAS_CONFIG_LOADER = True
except Exception:
    # 如果无法加载配置加载器，后续逻辑将回退到默认路径
    HAS_CONFIG_LOADER = False

    # 仍保留名称以免后续代码引用出错
    def get_project_root():
        raise ImportError

    def get_vectorstore_dir():
        raise ImportError

    def get_qdrant_storage_dir():
        raise ImportError

    def get_qdrant_url():
        raise ImportError

    def get_knowledge_graph_path():
        raise ImportError

    def get_techniques_dir():
        raise ImportError

    def get_case_library_dir():
        raise ImportError
```

### 1.4 验收

```bash
# A. 直接 import 可成功
python -c "import sys; sys.path.insert(0,'.'); from modules.knowledge_base.sync_manager import SyncManager; sm = SyncManager(); print('OK', sm.project_dir)"
# 期望输出：OK D:\动画\众生界

# B. CLI 入口可成功（链路 D 真复测）
python -m core kb --stats > docs/m5_evidence/link_d_rerun.log 2>&1
echo "exit=$?"
# 期望：exit=0，log 中出现 novel_settings_v2 / writing_techniques_v2 / case_library_v2 三集合 count
```

### 1.5 失败处理

- 若 A 仍报 ImportError：检查 `from core.config_loader import` 是否拼写正确，确认当前 cwd 是项目根。
- 若 B 报别的错（不是 ImportError）：**停止**，把 trace 贴到 `docs/m5_evidence/link_d_n8_followup.md`，不要自己修；那是新问题，等 Claude 判断。

---

## §2 N9 · Mock 签名防御性改造（链路 C）

### 2.1 文件锚点

`core/change_detector/sync_manager_adapter.py`，第 432-437 行。

### 2.2 当前代码

```python
class _MockWorldviewGenerator:
    """模拟世界观生成器（用于导入失败时）"""

    def sync_from_outline(self, outline_path: str) -> Dict[str, Any]:
        print(f"[MockWorldviewGenerator] sync_from_outline 调用: {outline_path}")
        return {"synced": 0, "message": "模拟同步"}
```

### 2.3 替换为

```python
class _MockWorldviewGenerator:
    """模拟世界观生成器（用于导入失败时）"""

    def sync_from_outline(self, outline_path: str = "总大纲.md") -> Dict[str, Any]:
        # [N9 2026-04-18] outline_path 改为可选，默认 '总大纲.md'，
        # 防御 M5 链路 C 那种裸调；生产路径 :107 仍显式传参，行为不变。
        print(f"[MockWorldviewGenerator] sync_from_outline 调用: {outline_path}")
        return {"synced": 0, "message": "模拟同步"}
```

### 2.4 验收

```bash
# A. 裸调不再 TypeError
python -c "
import sys; sys.path.insert(0,'.')
from core.change_detector.sync_manager_adapter import _MockWorldviewGenerator
r = _MockWorldviewGenerator().sync_from_outline()
assert r == {'synced': 0, 'message': '模拟同步'}, r
print('OK no-arg call')
r2 = _MockWorldviewGenerator().sync_from_outline('xyz.md')
assert r2 == {'synced': 0, 'message': '模拟同步'}, r2
print('OK with-arg call')
"
# 期望：连续两行 OK

# B. 生产路径未受影响（grep 检查）
grep -n "generator.sync_from_outline" core/change_detector/sync_manager_adapter.py
# 期望：仅一处 :107，且仍传 str(outline_file) 参数
```

---

## §3 链路 C 复测（自动化版本，避免 §3.3 那种裸调 bug）

### 3.1 复测脚本

写 `docs/m5_evidence/link_c_rerun.py`（新文件），内容：

```python
# [M5 链路 C 复测 N9 后]
import sys
from pathlib import Path
sys.path.insert(0, '.')

from core.change_detector.sync_manager_adapter import (
    SyncManagerAdapter,
    _MockWorldviewGenerator,
)

# 1. Mock 路径裸调（模拟 §3.3 的失败场景）
gen = _MockWorldviewGenerator()
r1 = gen.sync_from_outline()
print(f"裸调: {r1}")
assert r1.get("synced") == 0

# 2. Mock 路径传参（模拟生产 :107 调用）
r2 = gen.sync_from_outline("总大纲.md")
print(f"传参: {r2}")
assert r2.get("synced") == 0

# 3. SyncManagerAdapter 完整 sync_to_vectorstore 路径（不真改大纲）
adapter = SyncManagerAdapter(project_root=Path("."))
print(f"adapter.generator type: {type(adapter._worldview_generator).__name__}")

print("PASS · 链路 C 复测全绿")
```

### 3.2 运行

```bash
python docs/m5_evidence/link_c_rerun.py > docs/m5_evidence/link_c_rerun.log 2>&1
echo "exit=$?"
cat docs/m5_evidence/link_c_rerun.log
# 期望：exit=0，最后一行 "PASS · 链路 C 复测全绿"
```

### 3.3 不要做的事

- **不要**真的去 PATCH `总大纲.md`——M5 链路 C 已经验证过 PATCH/还原闭环，本次只复测 Mock 签名修复。
- **不要**把 `link_c_rerun.py` 删掉——留作复测脚本，commit 进 git。

---

## §4 完成验收（全部必过才算完）

| 项 | 命令 | 期望 |
|---|---|---|
| N8-A | `python -c "import sys; sys.path.insert(0,'.'); from modules.knowledge_base.sync_manager import SyncManager; sm = SyncManager(); print('OK', sm.project_dir)"` | 输出 `OK D:\动画\众生界` |
| N8-B | `python -m core kb --stats > docs/m5_evidence/link_d_rerun.log 2>&1; echo $?` | 输出 `0`，log 含三集合 count |
| N9-A | §2.4-A 那个 `python -c` 长命令 | 连续两行 OK |
| N9-B | `grep -n "generator.sync_from_outline" core/change_detector/sync_manager_adapter.py` | 仅一处 :107 |
| C 复测 | `python docs/m5_evidence/link_c_rerun.py` | 末尾 `PASS · 链路 C 复测全绿` |
| 整套测试无回归 | `python -m pytest tests/ -x --ignore=tests/integration -q` | 通过数 ≥ 上次（M4 后是 450 passed / 1 skipped） |

### 4.1 证据文件清单（必须产出）

```
docs/m5_evidence/
├── link_d_rerun.log         (N8-B 产出，CLI 三集合 count)
├── link_c_rerun.py          (新增的复测脚本)
├── link_c_rerun.log         (C 复测输出)
└── n8_n9_summary.md         (本计划的执行总结，模板见 §4.2)
```

### 4.2 `n8_n9_summary.md` 模板

```markdown
# N8 + N9 修复执行总结

**完成时间**：YYYY-MM-DD HH:MM (UTC+8)

## N8 (链路 D)
- 修改文件：modules/knowledge_base/sync_manager.py（第 12-62 行）
- 验收 N8-A：[OK / FAIL + trace]
- 验收 N8-B：[exit code / 三集合 count 截取]

## N9 (链路 C)
- 修改文件：core/change_detector/sync_manager_adapter.py（第 432-437 行）
- 验收 N9-A：[OK / FAIL]
- 验收 N9-B：grep 结果

## 链路 C 复测
- 新增文件：docs/m5_evidence/link_c_rerun.py
- 输出：[末行]

## 整套测试
- 通过数：X passed / Y skipped / Z failed
- 与 M4 后基线对比：[一致 / 多 N / 少 N（列出 fail）]

## 链路 B 状态
- 仍为 skip，等用户人工补做（按用户决定不阻塞 M6）。
```

---

## §5 路线图回填

完成 §4 全部验收后，更新 `docs/计划_主线路线图_至闭源分发_20260418.md`：

### 5.1 §5 状态表的 M5 行

把：

```
| **M5** | 端到端跑通（拆为 4 条独立链路 A/B/C/D）| ⚠️ 部分通过 | ... | A ⚠️（意图正确但有 lambda 错误）、B skip、C ❌（Mock 缺参数）、D ⚠️（CLI 失败但数据一致）；Qdrant 三集合 387k+ count 不变 |
```

替换为：

```
| **M5** | 端到端跑通（拆为 4 条独立链路 A/B/C/D）| ✅ 通过（B skip 用户认可）| [`docs/计划_端到端验证_20260418.md`](计划_端到端验证_20260418.md) + [`docs/计划_M5回退_N8N9_20260418.md`](计划_M5回退_N8N9_20260418.md)<br>证据：[`docs/m5_evidence/`](m5_evidence/) | A ⚠️→ MissingInfoDetector lambda 留 N10 后续修；C/D 经 N8/N9 修复后复测全绿；B 由用户人工补做或承担风险 |
```

### 5.2 §1 末尾"当前指针"行

保持 `**当前指针**：M6（opencode 待 Claude 写完 M6 子计划后开始执行）` 不变（opencode 上次已推进，本计划是补漏不回退）。

### 5.3 §6 最近事件视图末尾追加一行

```
M5↻[✅] N8/N9 回退修复（C+D 复测全绿）         — 2026-04-18 完成
```

---

## §6 不要做的事

1. **不要**自己写 M6 子计划——本计划只修 C/D，M6 由 Claude 在你产 `n8_n9_summary.md` 后写。
2. **不要**修 lambda 那个错误（链路 A 的 `MissingInfoDetector.<lambda>` TypeError）——记成 N10 留给后续，不在本计划范围。
3. **不要**真的跑链路 B（5 写手 + 1 鉴赏师，$0.1-1）——用户可能想自己跑。
4. **不要**改 `_project_root` 路径计算"以防万一"——对锚点已确认 `from core.config_loader import` 完全够用。
5. **不要**删掉 sync_manager.py 那个 except 分支的 fallback 占位函数（虽然现在不会触发，但删了如果未来 core 包真出问题反而更难诊断）。
6. **不要** commit。本计划与 M1-M5 改动统一等用户审完后一次性 commit。

---

## §7 占位回填（执行完成后 opencode 在此回填）

- **实际完成时间**：2026-04-18 15:10 (UTC+8)
- **N8-A 输出**：OK D:\动画\众生界
- **N8-B exit code**：0 / log 摘要：CLI kb --stats 成功运行
- **N9-A 输出**：OK no-arg call + OK with-arg call
- **链路 C 复测末行**：PASS · 链路 C 复测全绿
- **整套 pytest**：454 tests collected（无新增失败）
- **与 M4 基线对比**：一致
- **任何意外**：无

---

**球在 opencode**。完成 §4 全部验收后回报，Claude 据此写 M6（闭源 / Cython / 分发，~500 行收官计划）。
