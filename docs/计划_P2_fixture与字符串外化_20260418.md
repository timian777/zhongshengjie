# M4 · P2 · 测试修复 + intent_classifier 字符串外化

> **创建时间**：2026-04-18 03:55（上海时间）
> **里程碑编号**：主线路线图 **M4**
> **前置条件**：M3 已完成 ✅
> **后置触发**：opencode 完成此计划后 → 通知 Claude 写 **M5**（端到端验证）
> **执行模型**：opencode (GLM5)。所有步骤含完整代码块和精确锚点
> **预估工时**：~3 小时（测试修复 ~30min + 外化 ~2h + 回归 ~30min）

---

## 0. 关键发现（必读，决定本计划走向）

### 发现 1：路线图说的 fixture 问题已被 M2-α 解决

路线图原话："3 个测试 fixture 缺失（`tests/test_workflow_logic_full.py` 报 `.vectorstore/scene_writer_mapping.json` 不存在）"。

实际状态：
- `test_workflow_logic_full.py` 已在 M2-α 归档至 `.archived/tests_old_workflow_20260418/`，不再被 pytest 收集
- `scene_writer_mapping.json` 在 `config/scene_writer_mapping.json` 早就存在
- → **原 fixture 问题不复存在**

### 发现 2：M2-β 副作用产生新的 4 个测试问题

跑 `python -m pytest tests/ --tb=no -q` 实测：
```
454 collected, 450 passed, 1 failed, 3 errors  (99.1%)
```

4 个问题全部源自 M2-β 归档 `.vectorstore/core/`：

| # | 测试 | 失败原因 |
|---|------|---------|
| 1 | `tests/test_creation_context_api.py::test_save_stage_output` | `ModuleNotFoundError: creation_context_api`（已归档至 `.archived/vectorstore_core_20260418/creation_context_api.py`）|
| 2 | `tests/test_creation_context_api.py::test_query_context_returns_list` | 同上 |
| 3 | `tests/test_creation_context_api.py::test_clear_chapter_context` | 同上 |
| 4 | `tests/test_workflow_integration.py::test_workflow_method_documentation` | `FileNotFoundError: .vectorstore/core/workflow.py`（已归档），该测试只验证文档字符串存在，目标文件已没了 |

**修复策略**：
- `test_creation_context_api.py` 整文件归档（与 7 个 vectorstore 测试同 batch）
- `test_workflow_method_documentation` 单测试归档（注释整个函数体为 skip，保留其他 3 个 test_workflow_integration 测试）

修复后预期：454 collected → 451 collected（归档 3 个 test_creation_context），450 passed → 451 passed，0 failed，0 errors，**通过率 100%（≥99.4% 目标）**

### 发现 3：intent_classifier.py 字符串外化是 M6 Cython 编译前置

文件 `core/conversation/intent_classifier.py` 763 行，含 50 个意图、459+ 中文 regex 字符串，结构是两个 class attr 大字典：
- `CORE_INTENTS`（约 25 意图，line 63-377）
- `EXTENDED_INTENTS`（约 25 意图，line 379-573）

每个意图项形如：
```python
"start_chapter": {
    "patterns": [r"写第(.+?)章", r"开始创作第(.+?)章", ...],
    "category": IntentCategory.WORKFLOW_CONTROL,
    "entities": ["chapter"],
},
```

**为什么外化**：M6 闭源/Cython 编译时，纯算法逻辑可编为 `.pyd`，但中文 regex 字符串编入 `.pyd` 后**无法二次定制**（用户改 .py 已无源码）。审核报告 §5 把这类称"类 B 字符串"，必须先抽到 `.json` 让用户后续可独立配置。

**外化后期望**：
- `config/intent_patterns.json`：50 意图 × 459+ 字符串，**JSON 是唯一真相源**
- `intent_classifier.py`：删 `CORE_INTENTS`/`EXTENDED_INTENTS` 两个 class attr，改为 `_load_patterns_from_json()` 类方法在 `__init__` 时读取 JSON，逻辑层不动
- 现有公开 API（`classify`、`get_intent_info`、`get_all_intents`、`get_intents_by_category`）行为完全不变

### 发现 4：本计划 Part B 设计

为避免手动转写 459 个 regex 出错，**写一个一次性提取脚本**（`tools/extract_intent_patterns_to_json.py`），它 import `IntentClassifier` 然后把 `CORE_INTENTS`+`EXTENDED_INTENTS` 序列化为 JSON。规避手工抄写错漏，自动保证一致。

提取脚本完成使命后保留在 `tools/`（带"一次性"注释），不归档（万一日后又需要重抽）。

---

## 1. 步骤 1：建立测试基线

```bash
cd /d/动画/众生界
python -m pytest tests/ --tb=no -q 2>&1 | tail -5
```

**记录**到本计划末尾"步骤 1 基线"小节，格式：
```
基线（M3 完成后，M4 开始前）：
- collected: 454
- passed: 450
- failed: 1
- errors: 3
- pass rate: 99.1%
- 时间戳: <UTC+8>
```

---

## 2. Part A · 测试修复（4 处）

### 2.1 归档整个 `test_creation_context_api.py`

**操作**：
```bash
git mv tests/test_creation_context_api.py .archived/tests_old_workflow_20260418/test_creation_context_api.py
```

**校验**：
```bash
ls tests/test_creation_context_api.py 2>&1
# 期望：No such file or directory

ls .archived/tests_old_workflow_20260418/test_creation_context_api.py
# 期望：文件存在

git status --short | grep test_creation_context_api
# 期望：1 行 R rename
```

> **为什么整文件归档而不是修复**：`creation_context_api` 整个模块在 M2-β 已永久归档（详见 `.archived/vectorstore_core_20260418/README.md`），新框架未提供等价 API。该测试针对的实现已不存在，修复无意义。

### 2.2 跳过 `test_workflow_method_documentation` 单测试

**目标文件**：`tests/test_workflow_integration.py`
**精确锚点 - 旧代码**（line 34-41）：

```python
def test_workflow_method_documentation():
    """验证 workflow.py 中方法文档"""
    workflow_path = project_root / ".vectorstore" / "core" / "workflow.py"
    content = workflow_path.read_text(encoding="utf-8")

    assert "def get_phase1_dispatch" in content
    assert "Stage 4 Phase 1 灵感引擎分发" in content
    assert "from core.inspiration.workflow_bridge import phase1_dispatch" in content
```

**新代码**（精确替换）：

```python
@pytest.mark.skip(
    reason="[M4] .vectorstore/core/workflow.py 已在 M2-β 归档；"
    "本测试只验证文档字符串存在，目标文件不再维护。"
    "新框架的等价测试由 test_workflow_inspiration_branch.py 等覆盖。"
)
def test_workflow_method_documentation():
    """[已废弃] 原验证 .vectorstore/core/workflow.py 中方法文档"""
    pass
```

**前置导入检查**：确认 `tests/test_workflow_integration.py` 顶部已 `import pytest`。如无：
```bash
head -10 tests/test_workflow_integration.py | grep "^import pytest" || echo "需要在文件顶部添加 import pytest"
```
若无 `import pytest`，在文件第一行（在其他 import 之前）添加：
```python
import pytest
```

**校验**：
```bash
grep -n "@pytest.mark.skip" tests/test_workflow_integration.py
# 期望：1 行命中

python -c "import ast; ast.parse(open('tests/test_workflow_integration.py', encoding='utf-8').read()); print('syntax OK')"
# 期望：输出 syntax OK
```

---

## 3. Part B · intent_classifier 字符串外化

### 3.1 写一次性提取脚本

**新建文件**：`tools/extract_intent_patterns_to_json.py`

**完整内容**（精确粘贴）：

```python
#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
[M4 一次性脚本] 提取 IntentClassifier 的 CORE_INTENTS + EXTENDED_INTENTS 到 JSON

为什么存在：
- intent_classifier.py 含 459+ 中文 regex 字符串，M6 Cython 编译时这类字符串
  应外化到 JSON 让用户可独立修改（详见审核报告 §5「类 B 字符串外化」）
- 手抄 459 个 regex 极易出错，本脚本通过 import 直接序列化保证一致性

使用：
    python tools/extract_intent_patterns_to_json.py
    # 输出：config/intent_patterns.json

一次性：跑完一次产出 JSON 后基本不再用；保留以备日后需要重新提取。
"""

import json
import sys
from datetime import datetime
from pathlib import Path

PROJECT_ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(PROJECT_ROOT))

from core.conversation.intent_classifier import IntentClassifier, IntentCategory


def _serialize_intents(intents: dict) -> dict:
    """把 IntentCategory enum 转成字符串名，patterns 与 entities 原样保留"""
    out = {}
    for name, cfg in intents.items():
        category = cfg["category"]
        if isinstance(category, IntentCategory):
            category_name = category.name
        else:
            raise TypeError(
                f"Intent {name} 的 category 不是 IntentCategory: {type(category)}"
            )
        out[name] = {
            "patterns": list(cfg["patterns"]),
            "category": category_name,
            "entities": list(cfg.get("entities", [])),
        }
    return out


def main():
    output_path = PROJECT_ROOT / "config" / "intent_patterns.json"

    core = _serialize_intents(IntentClassifier.CORE_INTENTS)
    extended = _serialize_intents(IntentClassifier.EXTENDED_INTENTS)

    payload = {
        "_meta": {
            "version": "1.0",
            "extracted_from": "core/conversation/intent_classifier.py",
            "extracted_at": datetime.now().isoformat(timespec="seconds"),
            "milestone": "M4 (类 B 字符串外化)",
            "core_intents_count": len(core),
            "extended_intents_count": len(extended),
            "total_pattern_strings": sum(len(v["patterns"]) for v in core.values())
            + sum(len(v["patterns"]) for v in extended.values()),
        },
        "core_intents": core,
        "extended_intents": extended,
    }

    output_path.parent.mkdir(parents=True, exist_ok=True)
    with open(output_path, "w", encoding="utf-8") as f:
        json.dump(payload, f, ensure_ascii=False, indent=2)

    meta = payload["_meta"]
    print(f"[OK] 写入 {output_path}")
    print(
        f"  核心意图: {meta['core_intents_count']} | "
        f"扩展意图: {meta['extended_intents_count']} | "
        f"regex 总数: {meta['total_pattern_strings']}"
    )


if __name__ == "__main__":
    main()
```

**校验**：
```bash
ls tools/extract_intent_patterns_to_json.py
# 期望：文件存在
```

### 3.2 跑提取脚本生成 JSON

```bash
cd /d/动画/众生界
python tools/extract_intent_patterns_to_json.py
```

**期望输出**：
```
[OK] 写入 .../config/intent_patterns.json
  核心意图: <≥20> | 扩展意图: <≥20> | regex 总数: <≥459>
```

**校验**：
```bash
ls -la config/intent_patterns.json
# 期望：文件存在，size > 30KB

python -c "
import json
with open('config/intent_patterns.json', encoding='utf-8') as f:
    d = json.load(f)
print(f'core: {len(d[\"core_intents\"])}, extended: {len(d[\"extended_intents\"])}')
print(f'total patterns: {d[\"_meta\"][\"total_pattern_strings\"]}')
assert len(d['core_intents']) + len(d['extended_intents']) >= 40, '意图数过少'
assert d['_meta']['total_pattern_strings'] >= 400, 'regex 总数过少'
print('JSON 校验通过')
"
# 期望：所有 assert 通过
```

**如果 regex 总数 < 400**：停下，把脚本输出贴到本计划末尾"步骤 3.2 异常"小节，等用户处理（说明 IntentClassifier 实际 patterns 数量与审核报告 459 估计偏差较大，需重新评估）。

### 3.3 改写 `intent_classifier.py` 从 JSON 加载

**目标文件**：`core/conversation/intent_classifier.py`

**操作 A**：在文件顶部 `import re` 之后追加 import（约 line 18-22 之间）：

**精确锚点 - 旧代码**（line 18-22）：
```python
import re
from typing import Dict, List, Optional, Any, Tuple
from dataclasses import dataclass
from enum import Enum
```

**新代码**（精确替换）：
```python
import json
import re
from pathlib import Path
from typing import Dict, List, Optional, Any, Tuple
from dataclasses import dataclass
from enum import Enum
```

**操作 B**：在 `class IntentClassifier:` 定义之后、第一个 class attr 之前插入加载方法。

**精确锚点 - 旧代码**（约 line 59-63，含定位字符串 `class IntentClassifier:`）：
```python
class IntentClassifier:
    """意图分类器"""

    # 核心意图（高优先级）
    CORE_INTENTS = {
```

**新代码**（精确替换，**保留** `# 核心意图（高优先级）` 注释和后续整个 `CORE_INTENTS = {` 字典原样不动**）：
```python
class IntentClassifier:
    """意图分类器（M4 后 patterns 从 config/intent_patterns.json 加载）"""

    # [M4] 类 B 字符串外化：patterns 从 JSON 读取，类属性 CORE_INTENTS/EXTENDED_INTENTS 已废弃
    _PATTERNS_JSON_PATH = (
        Path(__file__).resolve().parents[2] / "config" / "intent_patterns.json"
    )

    @classmethod
    def _load_patterns_from_json(cls) -> Tuple[Dict, Dict]:
        """从 config/intent_patterns.json 加载 core+extended，把 category 字符串映回 IntentCategory enum"""
        if not cls._PATTERNS_JSON_PATH.exists():
            raise FileNotFoundError(
                f"intent_patterns.json 不存在: {cls._PATTERNS_JSON_PATH}\n"
                "请先运行 python tools/extract_intent_patterns_to_json.py"
            )
        with open(cls._PATTERNS_JSON_PATH, "r", encoding="utf-8") as f:
            data = json.load(f)

        def _restore(intents: dict) -> dict:
            out = {}
            for name, cfg in intents.items():
                cat_name = cfg["category"]
                try:
                    cat_enum = IntentCategory[cat_name]
                except KeyError:
                    raise ValueError(
                        f"intent_patterns.json 中 {name} 的 category={cat_name!r} "
                        f"不在 IntentCategory 枚举中"
                    )
                out[name] = {
                    "patterns": list(cfg["patterns"]),
                    "category": cat_enum,
                    "entities": list(cfg.get("entities", [])),
                }
            return out

        return _restore(data["core_intents"]), _restore(data["extended_intents"])

    # 核心意图（高优先级）
    CORE_INTENTS = {
```

**操作 C**：把 `__init__` 改为从 JSON 加载（替代 `self._all_intents = {**self.CORE_INTENTS, **self.EXTENDED_INTENTS}`）。

**精确锚点 - 旧代码**（约 line 575-581，`def __init__` 整体）：
```python
    def __init__(self):
        """初始化意图分类器"""
        self._all_intents = {**self.CORE_INTENTS, **self.EXTENDED_INTENTS}
        # 编译所有正则（性能优化）
        self._compiled_patterns: Dict[str, List[Tuple[re.Pattern, List[str]]]] = {}
        self._compile_patterns()
```

**新代码**（精确替换）：
```python
    def __init__(self):
        """初始化意图分类器（[M4] 从 config/intent_patterns.json 加载 patterns）"""
        core, extended = self._load_patterns_from_json()
        self._all_intents = {**core, **extended}
        # 类属性 CORE_INTENTS/EXTENDED_INTENTS 暂保留供向后兼容（如有外部代码读取）
        # 编译所有正则（性能优化）
        self._compiled_patterns: Dict[str, List[Tuple[re.Pattern, List[str]]]] = {}
        self._compile_patterns()
```

> **注意**：本步**不删** `CORE_INTENTS` 和 `EXTENDED_INTENTS` 两个 class attr。原因：
> - `tools/extract_intent_patterns_to_json.py` 仍要 import 它们做提取（万一未来再跑）
> - 任何外部代码若读 `IntentClassifier.CORE_INTENTS` 不会突然 break
> - JSON 才是真相源（`__init__` 从 JSON 读，class attr 即使过时也不参与运行时分类逻辑）
> - **真正删除**留到 M6 Cython 编译那一步（届时 .py 整体改写）

**校验**：
```bash
grep -n "_load_patterns_from_json\|_PATTERNS_JSON_PATH\|core, extended = self._load_patterns_from_json" core/conversation/intent_classifier.py
# 期望：≥3 行命中

python -c "from core.conversation.intent_classifier import IntentClassifier; c = IntentClassifier(); print(f'loaded {len(c._all_intents)} intents from JSON')"
# 期望：输出 loaded <≥40> intents from JSON

python -c "
from core.conversation.intent_classifier import IntentClassifier, IntentCategory
c = IntentClassifier()
r = c.classify('写第三章')
print(f'intent={r.intent} cat={r.category} entities={r.entities} conf={r.confidence:.2f}')
assert r.intent == 'start_chapter', f'分类结果异常: {r.intent}'
print('外化后行为一致性 OK')
"
# 期望：assert 通过，输出 OK
```

---

## 4. 步骤 4：回归测试，对比基线

```bash
python -m pytest tests/ --tb=short -q 2>&1 | tail -15
```

**判定规则**：
- ✅ 通过：`passed` ≥ 451；`failed` = 0；`errors` = 0
- ✅ 通过：`collected` 应为 451（基线 454 - 归档 3 = 451）
- ✅ 通过：通过率 = 100%（≥99.4% 目标）
- ❌ 失败：任何 `intent_classifier` 相关测试失败 → 回 §3.3 检查 JSON 是否完整
- ❌ 失败：任何 `creation_context_api` 残留引用 → 检查是否漏改

**如果失败**：把失败 trace 完整贴到本计划末尾"步骤 4 失败记录"小节，**停下，不 commit，不继续**，等用户处理。

---

## 5. 步骤 5：grep 兜底确认

```bash
echo "=== A. test_creation_context_api 已归档 ==="
ls tests/test_creation_context_api.py 2>&1
ls .archived/tests_old_workflow_20260418/test_creation_context_api.py

echo ""
echo "=== B. test_workflow_method_documentation 已 skip ==="
grep -B1 "def test_workflow_method_documentation" tests/test_workflow_integration.py | head -5

echo ""
echo "=== C. config/intent_patterns.json 存在且非空 ==="
ls -la config/intent_patterns.json
python -c "
import json
with open('config/intent_patterns.json', encoding='utf-8') as f:
    d = json.load(f)
print(f'_meta: {d[\"_meta\"]}')
"

echo ""
echo "=== D. intent_classifier.py 已接入 JSON 加载 ==="
grep -n "_load_patterns_from_json\|_PATTERNS_JSON_PATH" core/conversation/intent_classifier.py
```

**期望**：
- A 段：tests/ 中无、.archived/ 中有
- B 段：`@pytest.mark.skip` 装饰器存在
- C 段：JSON 存在，`_meta` 显示 ≥40 意图、≥400 patterns
- D 段：≥3 行命中

---

## 6. 步骤 6：更新主线路线图

**目标文件**：`docs/计划_主线路线图_至闭源分发_20260418.md`

**操作 1**：找到表格 §1 中 M4 那一行：
```
| **M4** | P2 · test fixture 补齐 + intent_classifier 459 个中文字符串外化到 `config/intent_patterns.json` | ⏳ 待 Claude 写子计划 | **待写** → 路径预定：`docs/计划_P2_fixture与字符串外化_20260418.md` | 测试通过率 ≥99.4% + 类 B 字符串入 JSON |
```

**替换为**：
```
| **M4** | P2 · 测试修复（M2-β 副作用 4 处）+ intent_classifier 字符串外化到 `config/intent_patterns.json` | ✅ 已完成 | [`docs/计划_P2_fixture与字符串外化_20260418.md`](计划_P2_fixture与字符串外化_20260418.md) | 测试通过率 100%（451 passed / 0 failed）+ 50 意图、≥459 regex 入 JSON、IntentClassifier 改为 JSON 加载 |
```

**操作 2**：找到 §5 状态总表中 M3/M4 行：
```
M3 [✅] P1-β M4 接入 + N5 合并（方案 A 反转）          — 完成
M4 [⏳] P2 fixture + 字符串外化                     — 子计划待 Claude 写 ← 当前
```

**替换为**：
```
M3 [✅] P1-β M4 接入 + N5 合并（方案 A 反转）          — 完成
M4 [✅] P2 测试修复 + 字符串外化                     — 完成
M5 [⏳] 端到端验证                                  — 子计划待 Claude 写 ← 当前
```

> 原 §5 已有 `M5 [⏳] 端到端验证 — 子计划待写` 行，**不要重复添加**——只是把"← 当前"标识从 M4 移到 M5。

**操作 3**：找到"**当前指针**"那行：
```
**当前指针**：M4（opencode 待 Claude 写完 M4 子计划后开始执行）
```

**替换为**：
```
**当前指针**：M5（opencode 待 Claude 写完 M5 子计划后开始执行）
```

---

## 7. 完成验收

opencode 完成全部步骤后贴出以下输出：

```bash
# A. 测试归档/skip 已生效
ls tests/test_creation_context_api.py 2>&1
grep -A1 "@pytest.mark.skip" tests/test_workflow_integration.py | head -3

# B. JSON 已生成
python -c "
import json
with open('config/intent_patterns.json', encoding='utf-8') as f:
    d = json.load(f)
print(f'core={len(d[\"core_intents\"])} extended={len(d[\"extended_intents\"])} patterns={d[\"_meta\"][\"total_pattern_strings\"]}')
"

# C. intent_classifier 已接入 JSON 加载
python -c "
from core.conversation.intent_classifier import IntentClassifier
c = IntentClassifier()
print(f'loaded {len(c._all_intents)} intents')
r = c.classify('写第三章')
print(f'classify smoke: {r.intent} ({r.category.name}) conf={r.confidence:.2f}')
"

# D. 步骤 4 测试结果（再跑一遍）
python -m pytest tests/ --tb=no -q 2>&1 | tail -5

# E. 路线图状态
grep -E "M4.*✅|当前指针" docs/计划_主线路线图_至闭源分发_20260418.md | head -5

# F. 工作树状态
git status --short | head -25
```

---

## 8. 不要做的事

- ❌ **不要** `git commit`——等用户审完 M1+M2+M3+M4 全部改动后统一提交
- ❌ **不要**修复 `test_creation_context_api.py` 让它通过——`creation_context_api` 模块已永久归档，新框架未提供等价 API，归档测试是正确动作
- ❌ **不要**删除 `IntentClassifier.CORE_INTENTS` / `EXTENDED_INTENTS` 两个 class attr——保留供 extractor 脚本和向后兼容（详见 §3.3 操作 C 注释）
- ❌ **不要**修改 `intent_classifier.py` 的任何业务逻辑（`classify`、`_match_patterns`、`_extract_entities` 等）——本计划仅做加载源切换，行为必须 100% 一致
- ❌ **不要**修改 `NOVEL_COLLECTION` / `TECHNIQUE_COLLECTION`（M3 已记此为遗留 bug，留给后续 M）
- ❌ **不要**改 `config/intent_patterns.json` 的内容——它是 extractor 自动产物，手改会与 .py 两边漂移
- ❌ **不要**自己写 M5 子计划——等 Claude 写
- ❌ 步骤 4 失败时**不要**自己尝试修复——停下，写到失败记录小节

---

## 步骤 1 基线
（opencode 在执行步骤 1 后填写）

```
基线（M3 完成后，M4 开始前）：
- collected: 454
- passed: 450
- failed: 1
- errors: 3
- pass rate: 99.1%
- 时间戳: 2026-04-18 (UTC+8)
- 注：test_creation_context_api.py 有 3 errors (EEE)
```

---

## 步骤 3.2 异常
（仅在 regex 总数 < 400 时填写）

```
（默认为空）
```

---

## 步骤 4 失败记录
（仅在测试回归失败时填写）

```
（默认为空）
```
