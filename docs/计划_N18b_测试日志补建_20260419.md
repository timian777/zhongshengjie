# 计划 P0-2 / N18b:n18_test_log.txt 补建

- **创建时间**:2026-04-19 (Asia/Shanghai)
- **执行者**:opencode (GLM5)
- **路线图位置**:[ROADMAP_众生界v2实施_20260419.md](./ROADMAP_众生界v2实施_20260419.md) §3.1 P0-2
- **依据**:[m7_artifacts/m7_summary.md](./m7_artifacts/m7_summary.md) §3.1 [N18-A'] + §5.3 "缺失产出"
- **前置**:P0-1 N18a **已完成**(顶层恰 8 子目录,Claude 于 2026-04-19 核验 PASS)
- **Shell 选择**:bash(与 [计划_M7_项目重构收口_20260418.md](./计划_M7_项目重构收口_20260418.md) §9.1 一致)

---

## 0. 本计划的目的(给 opencode 与未来 Claude)

2026-04-18 opencode 执行 M7 收口时,**漏了产出 `docs/m7_artifacts/n18_test_log.txt`**,导致 [N18-A'] 验收项卡在 🔴。诚实版 m7_summary §3.1 表格记录:

| 验收项 | 要求 | 实际 |
|--------|------|------|
| [N18-A'] 测试日志 | `docs/m7_artifacts/n18_test_log.txt` 存在且 ≥ 100 行 | **文件不存在** |

**本计划只做 1 件事**:按 [计划_M7_项目重构收口_20260418.md](./计划_M7_项目重构收口_20260418.md) §9.1 第 7 节(skill 层验证 N18)定义的命令,**真实重跑一次**,把完整输出 tee 到 `docs/m7_artifacts/n18_test_log.txt`,行数 ≥ 100。

**范围边界**(不得扩张):
- 只覆盖 [N18-A] / [N18-B] / [N18-C] / [N18-D] 4 项的命令输出(8 skill 中的 8 个已被 N18 原计划触及)
- **不做** [N18-E](worldview DEPRECATED 横幅)/ [N18-F](8 SKILL.md 文件大小变化 <30%)的深度补核——那是 **P0-3 N18c** 的工作
- **不做** 未覆盖 4 skill(inspiration-ingest / connoisseur / shared / technique-search)的处理——那是 **P0-4 N18d** 的工作
- 对这 2 部分只生成**占位说明**(见 §3.4 尾部 TODO 段),明确标注"由 N18c/N18d 处理"
- **不跑全量 pytest**(N21 任务已验证 467/1/0,不重复,节省时间;本计划聚焦 skill 层)

---

## 1. 执行前的真实状态(Claude 已于 2026-04-19 核验)

### 1.1 目标文件当前不存在

```bash
ls docs/m7_artifacts/
# 预期输出(3 条目,均非 n18_test_log.txt):
# m7_summary.md
# m7_summary.md.falsified_20260418.bak
# skill_backup_20260418
```

如果 `n18_test_log.txt` **已经存在**(被其他流程生成):立即停止,报 "CONFLICT: n18_test_log.txt 已存在,请作者处理",不要覆盖。

### 1.2 skill 真实状态(opencode 须知,不得假设)

- 8 个已处理的 skill:`novel-workflow`、`novelist-evaluator`、`novelist-canglan`、`novelist-jianchen`、`novelist-moyan`、`novelist-xuanyi`、`novelist-yunxi`、`novelist-worldview-generator`
- 位置:`C:/Users/39477/.agents/skills/<skill>/SKILL.md`(bash 下用正斜杠路径)
- **预期 grep 结果**(Claude 已预跑确认,opencode 重跑时应得到相同数字):
    - N18-B(novel-workflow 含 `from modules.creation`):**4 处**(lines 2103 / 2147 / 2179 / 2220)
    - N18-C(`.vectorstore/core` 在 novel-workflow + evaluator):**1 处**(novel-workflow line 15,注释字符串内;m7_summary §3.1 注明"字面未通过 / 实质通过")
    - N18-D([N18 2026-04-18] 在 8 个 skill):**每个 ≥ 1 处**
- 备份目录(N18a 已修复后):`docs/m7_artifacts/skill_backup_20260418/` 含 8 个子目录

---

## 2. opencode 执行规则(不可违反)

### 2.1 允许的操作

1. 新建 `docs/m7_artifacts/n18_test_log.txt`(空文件或追加)
2. 在项目根目录(`D:/动画/众生界`)运行 §3 定义的 bash 命令
3. 1 次最终 `wc -l` 自检 + 1 次 `head`/`tail` 抽检

### 2.2 禁止的操作

- ❌ **禁止修改任何 SKILL.md 文件内容**(本计划只读不写 skill)
- ❌ **禁止 git commit**
- ❌ **禁止 cp / mv / rm 除 `n18_test_log.txt` 外的任何文件**
- ❌ **禁止动 `~/.agents/skills/` 下任何文件**
- ❌ **禁止跑 pytest**(不必要,节省时间)
- ❌ **禁止"修复"任何 grep 结果出现的死引用**——本计划只留证据,修复是后续计划
- ❌ **禁止"顺便"补核 N18-E/N18-F**——那是 P0-3 的工作
- ❌ **不要用 PowerShell `Set-Content` 或 `Out-File` 重定向**——用 bash `tee -a` 保持 UTF-8 LF 一致

### 2.3 编码约束

- 日志文件必须 **UTF-8 无 BOM、LF 换行**(Linux 风格),便于 grep 与 diff
- bash 默认即满足;如 shell 是 Git Bash for Windows,确保不触发 CRLF 转换(`.gitattributes` 已有规则;但本文件不进 git,风险低)

---

## 3. 执行步骤(严格按顺序)

### 3.1 Step 1 — 初始化日志文件(覆盖写头部)

```bash
cd "D:/动画/众生界"
LOG="docs/m7_artifacts/n18_test_log.txt"

# 冲突检查
if [ -f "$LOG" ]; then
  echo "CONFLICT: $LOG 已存在,停止执行" >&2
  exit 1
fi

# 写入头部(覆盖)
cat > "$LOG" <<'HEADER'
================================================================================
 n18_test_log.txt — N18 skill 层验证日志(补建)
================================================================================
补建时间 : 2026-04-19 (Asia/Shanghai)
补建执行 : opencode (GLM5),按 docs/计划_N18b_测试日志补建_20260419.md §3
补建依据 : docs/计划_M7_项目重构收口_20260418.md §9.1 第 7 节 + §6.7 验收清单
覆盖范围 : [N18-A] / [N18-B] / [N18-C] / [N18-D],共 4 项
延后范围 : [N18-E] / [N18-F] 由 P0-3 N18c 补核
           未覆盖 4 skill(inspiration-ingest / connoisseur / shared /
           technique-search) 由 P0-4 N18d 处理
数据基线 : m7_summary.md (诚实版,2026-04-19)
================================================================================

HEADER
```

### 3.2 Step 2 — [N18-A] 备份目录结构(N18a 已修复后)

```bash
{
  echo ""
  echo "################################################################################"
  echo "# [N18-A] 备份目录结构(P0-1 N18a 已修复,预期 8 子目录)"
  echo "################################################################################"
  echo ""
  echo "--- 顶层条目 ---"
  ls docs/m7_artifacts/skill_backup_20260418/
  echo ""
  echo "--- 顶层计数(预期 = 8)---"
  ls docs/m7_artifacts/skill_backup_20260418/ | wc -l
  echo ""
  echo "--- novel-workflow 子目录内容(预期 5 条目:4 .md + 1 templates/)---"
  ls docs/m7_artifacts/skill_backup_20260418/novel-workflow/
  echo ""
  echo "--- 顶层散文件数(预期 = 0)---"
  find docs/m7_artifacts/skill_backup_20260418/ -maxdepth 1 -type f | wc -l
  echo ""
  echo "[N18-A] 判定:PASS(N18a 已于 2026-04-19 由 Claude 核验,本次记录留痕)"
} >> "$LOG" 2>&1
```

### 3.3 Step 3 — [N18-B] novel-workflow 含 4 处 `from modules.creation`(try-except 包裹)

```bash
{
  echo ""
  echo "################################################################################"
  echo "# [N18-B] novel-workflow/SKILL.md 保留 4 处 from modules.creation(try 包裹)"
  echo "################################################################################"
  echo ""
  echo "--- grep -n \"from modules.creation\" ---"
  grep -n "from modules.creation" "C:/Users/39477/.agents/skills/novel-workflow/SKILL.md" || echo "(无匹配)"
  echo ""
  echo "--- grep -c (预期 = 4)---"
  grep -c "from modules.creation" "C:/Users/39477/.agents/skills/novel-workflow/SKILL.md"
  echo ""
  echo "--- 每处 from modules.creation 前 2 行是否含 try:(人工检阅)---"
  grep -B 2 "from modules.creation" "C:/Users/39477/.agents/skills/novel-workflow/SKILL.md"
  echo ""
  echo "[N18-B] 判定:若 grep -c == 4 且每处前含 try: → PASS"
} >> "$LOG" 2>&1
```

### 3.4 Step 4 — [N18-C] `.vectorstore/core` 死引用清零(novel-workflow + evaluator)

```bash
{
  echo ""
  echo "################################################################################"
  echo "# [N18-C] .vectorstore/core 死引用(novel-workflow + evaluator)"
  echo "# 预期:0 行(严格)/ m7_summary 诚实版注明:novel-workflow 有 1 处在注释中,实质通过"
  echo "################################################################################"
  echo ""
  echo "--- grep -rn .vectorstore/core ---"
  grep -rn "\.vectorstore/core" \
    "C:/Users/39477/.agents/skills/novel-workflow/" \
    "C:/Users/39477/.agents/skills/novelist-evaluator/" \
    || echo "(无匹配,严格 PASS)"
  echo ""
  echo "--- 按 m7_summary §3.1 说明:novel-workflow 第 15 行是 [N18 2026-04-18] 注释说明,非活引用 ---"
  sed -n '15p' "C:/Users/39477/.agents/skills/novel-workflow/SKILL.md"
  echo ""
  echo "[N18-C] 判定:字面 1 处(注释) / 实质 PASS(无活死引用)"
} >> "$LOG" 2>&1
```

> **说明**:`grep -rn` 在 Git Bash 下支持 Windows 路径。若 opencode shell 不同导致失败,可退化为逐文件 `grep -n "\.vectorstore/core" <file>`。

### 3.5 Step 5 — [N18-D] 8 个 skill 均含 `[N18 2026-04-18]` 降级标记

```bash
{
  echo ""
  echo "################################################################################"
  echo "# [N18-D] 8 个已处理 skill 均含 [N18 2026-04-18] 标记"
  echo "################################################################################"
  for s in novel-workflow novelist-evaluator novelist-canglan novelist-jianchen novelist-moyan novelist-xuanyi novelist-yunxi novelist-worldview-generator; do
    f="C:/Users/39477/.agents/skills/$s/SKILL.md"
    echo ""
    echo "--- $s ---"
    if [ -f "$f" ]; then
      hits=$(grep -c "N18 2026-04-18" "$f")
      echo "命中行数:$hits"
      grep -n "N18 2026-04-18" "$f" | head -5
      [ "$hits" -ge 1 ] && echo "[$s] PASS" || echo "[$s] FAIL — 缺 [N18 2026-04-18] 标记"
    else
      echo "[$s] FAIL — SKILL.md 文件不存在"
    fi
  done
  echo ""
  echo "[N18-D] 判定:若全部 8 个均 PASS → PASS"
} >> "$LOG" 2>&1
```

### 3.6 Step 6 — 附录 A:8 个 SKILL.md 当前文件大小(为 P0-3 [N18-F] 预留基线)

```bash
{
  echo ""
  echo "################################################################################"
  echo "# 附录 A:8 个 SKILL.md 当前文件大小快照(为 P0-3 N18c [N18-F] 留基线)"
  echo "# 本步骤只采集数据,不做判定——判定由 P0-3 执行"
  echo "################################################################################"
  echo ""
  printf "%-40s %10s  %8s\n" "SKILL" "bytes" "lines"
  echo "--------------------------------------------------------------------------"
  for s in novel-workflow novelist-evaluator novelist-canglan novelist-jianchen novelist-moyan novelist-xuanyi novelist-yunxi novelist-worldview-generator; do
    f="C:/Users/39477/.agents/skills/$s/SKILL.md"
    if [ -f "$f" ]; then
      bytes=$(wc -c < "$f" | tr -d ' ')
      lines=$(wc -l < "$f" | tr -d ' ')
      printf "%-40s %10s  %8s\n" "$s" "$bytes" "$lines"
    else
      printf "%-40s %10s  %8s\n" "$s" "MISSING" "MISSING"
    fi
  done
  echo ""
  echo "(备份侧基线见 docs/m7_artifacts/skill_backup_20260418/<skill>/SKILL.md;对比由 P0-3 做)"
} >> "$LOG" 2>&1
```

### 3.7 Step 7 — 附录 B:TODO 占位(明确 P0-3 / P0-4 尚未处理)

```bash
{
  echo ""
  echo "################################################################################"
  echo "# 附录 B:后续待办(本计划不执行,仅登记)"
  echo "################################################################################"
  echo ""
  echo "[TODO-N18c / P0-3] [N18-E] worldview-generator 头部 ⚠️ DEPRECATED 横幅核验"
  echo "[TODO-N18c / P0-3] [N18-F] 8 个 SKILL.md 相对备份的大小变化 < 30%"
  echo "[TODO-N18d / P0-4] 4 个未覆盖 skill 清理:"
  echo "                   - novel-inspiration-ingest"
  echo "                   - novelist-connoisseur      (v2 核心,P1-3 将大改)"
  echo "                   - novelist-shared"
  echo "                   - novelist-technique-search"
  echo ""
  echo "################################################################################"
  echo "# 日志结束"
  echo "################################################################################"
  echo "生成完成时间(本机时间,参考):$(date '+%Y-%m-%d %H:%M:%S %Z')"
} >> "$LOG" 2>&1
```

---

## 4. 自检命令(执行完 §3 后必须跑,任一失败立即报错并停止)

```bash
cd "D:/动画/众生界"
LOG="docs/m7_artifacts/n18_test_log.txt"

# 自检 1:文件存在
test -f "$LOG" || { echo "FAIL-1: $LOG 不存在"; exit 1; }

# 自检 2:行数 ≥ 100
lines=$(wc -l < "$LOG" | tr -d ' ')
echo "总行数:$lines"
[ "$lines" -ge 100 ] || echo "FAIL-2: 行数=$lines,要求 ≥ 100"

# 自检 3:头部含补建时间
grep -q "补建时间 : 2026-04-19 (Asia/Shanghai)" "$LOG" || echo "FAIL-3: 头部缺补建时间"

# 自检 4:4 个 N18 章节标题都在
for tag in "N18-A]" "N18-B]" "N18-C]" "N18-D]"; do
  grep -q "\[$tag" "$LOG" || echo "FAIL-4: 缺 [$tag 章节"
done

# 自检 5:[N18-B] grep -c 输出 = 4(诚实版一致)
# 在 Step 3 的"grep -c (预期 = 4)"下一行(或附近)应看到数字 4
grep -A 1 "预期 = 4" "$LOG" | grep -q "^4$" || echo "FAIL-5: [N18-B] grep -c 不是 4"

# 自检 6:[N18-D] 8 个 skill 每个至少 1 条 "[<skill>] PASS" 或 FAIL 行
pass_count=$(grep -c "^\[.*\] PASS$" "$LOG")
echo "[N18-D] PASS 条数:$pass_count(预期 = 8)"
[ "$pass_count" -eq 8 ] || echo "FAIL-6: [N18-D] PASS 条数=$pass_count"

# 自检 7:附录 A 的大小表至少 8 行数据
size_rows=$(grep -cE "^(novel-workflow|novelist-)" "$LOG")
echo "附录 A 数据行数:$size_rows(预期 ≥ 8)"
[ "$size_rows" -ge 8 ] || echo "FAIL-7: 附录 A 数据行数不足"

# 自检 8:附录 B 含 4 个 TODO-N18d 条目的 skill 列表
for s in novel-inspiration-ingest novelist-connoisseur novelist-shared novelist-technique-search; do
  grep -q "$s" "$LOG" || echo "FAIL-8: 附录 B 缺 $s"
done

# 自检 9:项目结构未被动(防止误操作)
test -d core/inspiration || echo "FAIL-9: 项目结构被意外破坏"
test -f docs/m7_artifacts/m7_summary.md || echo "FAIL-10: m7_summary.md 被意外删除"

# 自检 10:无 git commit(HEAD 不变)
# (opencode 无需检查,Claude 在审阅时会核 git log)

echo ""
echo "ALL CHECKS DONE — 若上方无 FAIL 字样则全部通过"
echo "请把总行数($lines)与 PASS 条数($pass_count)报给作者"
```

任一 `FAIL-` 出现 → 立即停止,**不要尝试自动修补**,把 FAIL 行原样报告,等待 Claude 或作者介入。

---

## 5. 完成判据(作者审阅用)

- [x] `docs/m7_artifacts/n18_test_log.txt` 存在
- [x] 文件行数 ≥ 100
- [x] 含 [N18-A] / [N18-B] / [N18-C] / [N18-D] 4 个章节
- [x] [N18-B] grep -c 输出 = 4
- [x] [N18-C] 记录了 novel-workflow 第 15 行的注释说明(实质通过)
- [x] [N18-D] 8 个 skill 全部 PASS
- [x] 附录 A 含 8 个 skill 大小快照(为 P0-3 [N18-F] 留基线)
- [x] 附录 B 明确列出 P0-3 / P0-4 的 TODO
- [x] 其他 8 个 skill SKILL.md **未被修改**(`git status` 对 `~/.agents/skills/` 无感,但 `docs/m7_artifacts/` 仅新增 1 文件)
- [x] 无 git commit

---

## 6. 完成后更新 ROADMAP

完成后,opencode 须:

1. 把 [ROADMAP_众生界v2实施_20260419.md](./ROADMAP_众生界v2实施_20260419.md) §3.1 P0-2 行末的 🔴 改为 ✅
2. 把 §3 顶部"★ 当前任务指针"从 **P0-2** 推进到 **P0-3 N18c [N18-E]/[N18-F] 补核**
3. 在 §5 时间线追加一行:

```
| 2026-04-19 | opencode | N18b n18_test_log.txt 补建完成 | docs/m7_artifacts/n18_test_log.txt ≥ 100 行,[N18-A/B/C/D] 全 PASS |
```

下一步:等待 Claude 写 **P0-3 N18c** 计划(含 [N18-E] worldview DEPRECATED 横幅补核 + [N18-F] 8 SKILL.md 大小变化对比)。

---

**计划结束。opencode 按 §3.1 → §3.2 → ... → §3.7 顺序执行,§4 自检,§6 更新 ROADMAP。**
